# Primer — Beit V'Metaplim

> Last updated: 2026-06-11 (first-registration Master pitch popup) by Claude Code (**NEW POPUP `master_welcome_pitch` BUILT + 🔴 CRITICAL ADJACENT BUG FIXED — staged locally, NOT pushed (awaiting Hillel).** Hillel: every FIRST-TIME registrant gets his Ram-voice pitch message ("אם יצא לכם לצפות בקורס PRACTITIONER... 11,600 ש״ח... 1,900 ש״ח + בונוסים...") then routes to the in-portal sales page. **Built in `course-library-v2.html`:** modal `lmsMasterPitchModal` (verbatim text, dark-glass style, scrollable); registered in PopupManager (engagement, free_user, double role-guard); armed from `checkPaidRole()` — ONLY free-role users whose auth `created_at >= 2026-06-11` (new registrants, NOT the 714 existing), once EVER per user (localStorage key `lms_master_pitch_seen_<uid>`, set on show), 10s delay after landing; CTA → `showMasterSalesView()`. **DB row LIVE** (`popup_configs.master_welcome_pitch`, trigger_event=manual, status=live — admin kill-switch; applied via PostgREST service_role upsert — Management API token in soul-code.env got 403 on this project; SQL kept at `sql/master_welcome_pitch_popup.sql`, NOT a migration per DB freeze). **🔴 ADJACENT BUG (launch-critical, fixed):** ALL 18 inline onclick/onsubmit handlers in course-library-v2 were DEAD in prod (ReferenceError) — page logic lives in 3 IIFEs, nothing was exposed on window: auth-modal signup/Google-login/email-auth, questionnaire submit/skip, training_cta close+CTA (the money click! also its `goToMasterCheckout` call crossed IIFE scopes = silent no-op), share buttons. Fixed via `Object.assign(window,{...})` exposure block + window.showWelcome/showMasterSalesView/goToMasterCheckout. **Headless-verified (scripts/smoke_master_pitch.py + scripts/_check_globals.py):** 18/18 global, 0 JS errors, popup shows→once-flag set→CTA lands on #master-sales→modal closes. See hindsight 2026-06-11 (IIFE exposure rule). **AWAITING HILLEL:** (a) push portal commit (Vercel auto-deploy); (b) `fly deploy` crm-bot for the new buyer welcome message (00a15cf); (c) 🔴 Green API 7103515939 renewal — expires TOMORROW 12.6.)

> Last updated: 2026-06-11 (open-to-all purchase verification, scale-turbo) by Claude Code (**MASTER FUNNEL RE-VERIFIED FOR PUBLIC OPENING (6 parallel agents, all live probes) + NEW BUYER WELCOME TEMPLATE STAGED.** Hillel: "נותנים לכולם אפשרות רכישה של המאסטר — תוודא שהכל עובד". **ALL GREEN:** (1) Sales page open to ALL members — `sbMasterSalesItem` visible (no display:none/CSS hiding/role guard; the "ADMIN ONLY" comment at ~3583 is stale, code is open), `MASTER_CHECKOUT_URL` wired, consent gate + sticky bar present, 0 YT ID leaks. (2) Cardcom page 200 (Ram's merchant), webhook fail-closed 403, `CARDCOM_WEBHOOK_TOKEN`+`PARTNER_PHONE` deployed, Fly machine started, /health/reminders ok. (3) Content gate 6/6: get-master-lessons/get-lessons/ai-chat all anon-401, master transcripts+primer+hindsight blocked, summaries serve paid-gate.js, game has GAME_REQUIRE_PAID. (4) DB: subscriptions alive — only David (1,500₪, end 2099 ✓), **0 legacy pre-2030 rows (the one-time 2099 fix is moot — already clean)**, paid_customer=1/admin=3, training_cta popup live (free_user, ≥5 lessons), welcome-queue cron every minute (10 failed sends 06-10 = orphaned test profiles from the E2E sessions, not real buyers). (5) Bot code: price 1900 default/lifetime 2099/magic-link → `?view=master` all confirmed; auto-expire predicate `lt(end_date,now)` safe vs 2099. **NEW WELCOME MESSAGE (crm-bot `00a15cf`, committed NOT deployed):** Hillel's verbatim wording ("היי {שם פרטי} וברוכים הבאים למסע שינוי חיים...") replaced BOTH buyer templates (`cardcomBuyerMessage` + manual `welcomeMessage` → shared `masterWelcome()`) in `crm-bot/src/templates/paid-customers.js`; support phones rendered from config (הילל 054-911-6092 / רם 052-789-2299, env-overridable); link = magic link (auto) / portal URL (manual); render-tested both paths. **AWAITING HILLEL: `fly deploy` approval to go live.** **🔴 LAST BLOCKER: Green API 7103515939 expires TOMORROW 12.6** — both instances authorized today, BOTH share the same WhatsApp number (wid 972512940781, same webhook), bot instance 7103533485 is free-tier with quota history → renewal is the single point of failure for buyer welcomes; only Hillel can renew (paid, console.green-api.com). Minor: signed_contracts=0 rows is BY DESIGN (checkout terms checkbox replaced signature flow).)

> Last updated: 2026-06-11 (Elazar purge) by Claude Code (**ELAZAR BRAND PURGE — PUSHED `5ec21d5` (pre-push validation passed) + `ai-chat` REDEPLOYED, ALL LIVE-VERIFIED:** live nlp-game-master.html serves the Ram marker, 0 "אלעזר" in the live game page + m1 data + knowledge js, ai-chat anon still 401 fail-closed. Hillel approved ("צריך טיהור לפני שמרימים" → "תדחוף"). Mapped all 156 occurrences across 27 files, then acted by surface: (1) **Master game data m1/m2/m3/m6/m8/m9 (89 occurrences):** "אלעזר" → generic "המרצה" — deliberately NOT "רם", because the content quotes Elazar's PERSONAL anecdotes from the old transcripts (army religious-exemption story, 14h gaming, introvert story) and attributing them to Ram would be false; one awkward context line hand-fixed. Validated: `node --check` clean ×7 + `scripts/validate-games.js` → PRACTITIONER 312 + MASTER 311 exercises, 0 errors. (2) **AI persona → Ram:** `ai-chat/index.ts` SYSTEM_PROMPT_MASTER_HEADER now "אתה רם" (dropped HNLP claim — that was Elazar's credential), `knowledge-master.ts` instructor lines + identity story neutralized, `js/nlp-game-knowledge-master.js` "בסגנון רם", `nlp-game-master.html` "קורס המאסטר של רם". (3) **content-leverage** (marketing content engine): רם-only in CLAUDE.md/מה-זה.txt/avatar; generate_hooks.py untouched (Elazar there only in DO-NOT-MENTION prohibition lists — useful). (4) **docs/business old-program docs** (overview/business-model/faq/atomic-launch-package, 8,800₪-era, HNLP-via-Elazar claims): NOT rewritten (would falsify history) — added "ארכיון · לא לייצר מזה קופי" banner pointing to strategy-map-2026-06-11.md; personas founder line trimmed. (5) **NOT touched on purpose:** retired pages training-v2/3/4.html + master.html (deploy-blocked, full Elazar bios — quarantine candidates, Hillel's call); "אלעזר" in meta-campaign + portal-registrants city stats = the TOWN אלעזר, not the person; primer/hindsight history. Memory updated (project_beit_vmetaplim_elazar rewritten: Elazar OUT, Ram sole talent; elazar-approved-talent marked בוטל). **NEXT on approval:** `git push` (pre-push game validation will re-run) + `npx supabase functions deploy ai-chat --no-verify-jwt`, then a quick live probe that the master AI tutor answers as רם.)

> Last updated: 2026-06-11 (strategy) by Claude Code (**ADAPTED STRATEGY MAP WRITTEN — `docs/business/strategy-map-2026-06-11.md` (deploy-blocked via `/docs/business/`).** Hillel brought a full business-strategy doc (hybrid Freemium, AI teacher, data-for-value exchange, behavioral WhatsApp arms, liquidation safety net) and asked to adapt it to the system's current reality, **keeping everything Ram personal-brand at this stage**. The map: (1) locks the non-negotiable anchors (single product Master 1,900₪ lifetime, Practitioner free forever, personal-development positioning, no hard countdown, existing-audience-first, cert deferred, Ram-only talent, DB freeze, cost-gating rule); (2) shows ~70% of the strategy's infrastructure ALREADY EXISTS (EdTech wrapper+game+journey panel, training_cta popup, Cardcom E2E automation proven with a real sale, crm-bot arms, referrals, UTM/Pixel/CAPI); (3) corrects the source doc in 7 places (48h OTO→soft framing per Ram, cert station→phase 3, free AI 15-msg quota→needs Hillel spend approval, aggressive CPL→phase 3, 50₪ liquidation workshop→Ram decision vs value-stack cannibalization, Elazar purge needed in ~33 files incl. AI knowledge-master.ts + master game data); (4) phased gap plan — **Phase 1:** onboarding "why/goal" capture (new questionnaire field, manual idempotent SQL), welcome-pitch popup (PopupManager config), goal-aware contextual retention messages (crm-bot + lesson→promise map from transcripts), cart-abandonment = master-CTA click (popup_events) + no purchase-webhook within 45min → one soft WhatsApp, 10+-lesson outreach report in admin; **Phase 2:** AI profiling gate on the existing game mentor + 15-msg quota (ai_chat_usage already counts) + deep tracking (debt-note §3); **Phase 3:** paid scale, liquidation workshop, certification, back-end hub. **Quality blockers gating any hard sell:** broken Master videos (lessons 5/6/8/9 black segments — full sweep rerun + rebuilds), Green API expires 12.6, end_date→2099 one-time fix. 5 open decisions for Hillel+Ram listed in the doc. No code/DB changes this session — strategy documentation only.)

> Last updated: 2026-06-11 (13:45) by Claude Code (**EMAIL SENDING CAPABILITY — new `send-email` Edge Function DEPLOYED + E2E-verified, admin-UI compose modal STAGED (committed locally, NOT pushed — awaiting Hillel approval).** Hillel: "לחבר אפשרות שליחת מיילים דרך המערכת". Built the missing server piece: (1) **Edge Function `send-email`** (deployed `--no-verify-jwt`, secrets `GMAIL_API_URL`+`GMAIL_API_TOKEN` set): admin-JWT gate (admin-delete-user pattern), accepts `{to, subject, message≤2000}`, wraps message in a lean branded RTL template (petrol header + gold line), delivers via the existing Gmail Apps Script web app (GET — POST breaks on Google redirect), sender name "בית המטפלים" from htjewelry.a474@gmail.com, audits every send to `crm_activity_log` (`admin_send_email`). **E2E-verified live** (`scripts/test_send_email_e2e.py`: temp admin → send → 200 success → audit row written → inbox-verified the email actually arrived branded → temp user deleted; anon = 401). (2) **Admin UI (staged):** new `js/admin/admin-email.js` — shared compose modal `openEmailCompose({to,name})` (to/subject/message + char counter + toasts); gold "שלח מייל" button in the portal-lead modal (`emailPortalLead`, only when lead has email) + 📧 button per paid-customer row (`emailSub`); script tag added to admin.html (+cache-bust pq v=8, paid v=2). (3) **🐞 ADJACENT BUG FIXED:** the daily backup's email report + check_green_api_quota alerts NEVER sent — they called `action=sendEmail`/`htmlBody` but the Apps Script only knows `action=send`/`html`, and backup's success check (`"success" in result`) treated the error JSON as OK. Both scripts fixed (+JSON-parsed success check, emoji stripped from subjects per gmail-api rule — see hindsight 2026-06-11). **QUOTA NOTE:** consumer Gmail ≈100 emails/day, Apps Script ~10 sends/min — fine for CRM/transactional, NOT for broadcast (broadcast would need Resend/domain — future decision). **PUSHED + LIVE-VERIFIED** (`fc109de`+`55b2432` → prod; admin-email.js 200 with openEmailCompose, admin.html serves it + pq v=8). **RESEND MIGRATION IN PROGRESS (resume-state in memory `beit-vmetaplim-resend-email-migration`):** provider-chain deployed (`1bd89db` — Resend primary when RESEND_API_KEY secret set, Gmail fallback; E2E re-verified via gmail path, audit logs `provider`). Resend account OPEN, API key saved in `.env.local` (`RESEND_API_KEY`), domain therapist-home.com REGISTERED in Resend (id `1a8f3f7d-7003-4241-bad1-a56f6401730e`, eu-west-1). **PAUSED ON HILLEL:** add 3 DNS records at the Interspace panel (https://ng.interspace.net — NS are ns1/ns2.sitesdepot.com); records file: Desktop `רשומות-DNS-לריסנד.txt` + Claude-Chrome prompt given in chat. When he says "הוספתי": verify domain via Resend API → set `RESEND_API_KEY` Supabase secret → rerun `scripts/test_send_email_e2e.py` (expect provider=resend, from no-reply@therapist-home.com). Also still pending: browser-verify the compose modal visually in the live admin.)

> Last updated: 2026-06-11 (11:05) by Claude Code (**AUTOMATION-ENGINE HEALTH AUDIT (all green) + WELCOME-MSG FIX + ADMIN DELETE-USER FEATURE — deployed & live-verified, commits `34f4f6e`+`d69fe69` pushed.** (1) **Engine audit, all layers verified live:** crm-bot `/health/reminders` ok (3 sent/24h, heartbeat alive); both Green API instances `authorized`; pg_cron 3/3 jobs 100% success 48h (capi-queue, welcome-queue, meta-ads-spend); the 3 subscription crons (auto_expire/30day/expiry_today) run daily in crm-bot — last runs same morning. Resolved a false alarm: `auto_smart_automations_tick`/`auto_new_lead_polling` log entries stopped 06-07 06:58 — that's the intentional `SILENT_ON_SUCCESS` noise-reduction (deployed 06-07); rules provably alive (`smart_automation_run` 2,167/week, last 07:20 today). (2) **Welcome WhatsApp template fixed** (`send-welcome-whatsapp/index.ts` line ~83, redeployed): the "זומים חודשיים" bullet was a false promise → replaced with "עדכונים חשובים על מבצעים מיוחדים" (Hillel's wording). Single source of truth confirmed — welcome-queue-processor reuses this function, no duplicate template. (3) **NEW: admin dashboard user deletion** — new Edge Function `admin-delete-user` (deployed `--no-verify-jwt`): caller-JWT must be role=admin (ga4-analytics pattern); accepts `{user_id}` (full delete) / `{questionnaire_id}` (anon lead) / `{contact_request_id}` (contact-form lead); deletion order respects FK map (automation_rules.created_by + retention_messages.approved_by NO-ACTION → nulled; portal_questionnaires deleted explicitly since FK is SET NULL; profiles deleted explicitly [NO ACTION, cascades subscriptions]; `auth.admin.deleteUser` last → cascades progress/game/notes/referrals); refuses role=admin targets (403); audits every delete to `crm_activity_log` (`admin_delete_user`). UI: trash button per row + "מחק משתמש" in the lead modal (`js/admin/admin-portal-questionnaires.js`, `deletePortalLead()` — routes ALL deletes through the function, Hebrew confirm listing exactly what gets erased). **E2E-tested live with disposable users:** real delete verified gone from auth+profiles+audit row written; delete-admin → 403; no-token → 401; test users cleaned up. Pushed (pre-push game validation passed) + polled prod until `deletePortalLead` served. **GOTCHAS:** beit admin password is NOT the phone convention (login failed — tested via temp admin instead, good security); raw PostgREST returned 404 on `crm_activity_log`/`profiles` writes from curl (default schema quirk — used Management API SQL instead, supabase-js unaffected). **🔴 STILL OPEN:** Green API 7103515939 expires TOMORROW 12.6 — renew in console or welcome/reminder WhatsApp dies; existing paid customers' end_date one-time 2099 fix still pending.)

> Last updated: 2026-06-11 (00:30) by Claude Code (**MASTER LESSON-1 VIDEO REPLACED — the published video was BROKEN (11 min of black) — new clean rebuild LIVE, commit `97f1596` + `get-master-lessons` redeployed.** Hillel asked to verify the first Master video opens professionally. Found worse: published `Rab2Wzcon34` (29:16) had **baked-in black video 8:54-16:08 + 23:05-end** (audio fine, 1 bright frame/~20s) — shipped broken to every paid customer. Root cause: the Master_Final production re-encode read its source from OneDrive → silent black inter-frames (NightAuditor failure mode); QA missed it because black is Y=16 (video-range) and `blackdetect pix_th=0.05` (luma<12.75) can't see it. Upstream sources all clean (raw + Cleaned + Trimmed_v2). **Rebuilt from raw** (`מפגש 1 - מאסטר.mp4` 720p→1080p, segments per master_L01_editor.py recipe for parts 1+2): branded premium intro card (zoom + ambient music) → opens straight on the hook "מי כאן הגיע לקורס בשביל התפתחות אישית?" (cut 54s of mid-conversation chatter/projector/break-logistics that previously opened the video), acompressor+loudnorm → exactly -14 LUFS. Verified: 0 black (pix_th=0.10), YAVG bright at 5 sample points, 29:42. New YT `I3qjx4Xi62s` (same playlist, unlisted; old left unlisted as rollback). Swapped in 3 places: `get-master-lessons/index.ts` (deployed, anon-401 verified), `course-library.html` masterModules, `master-practice.html` clip. WhatsApp update sent to Hillel with link. **OPEN — 🔴 SWEEP PARTIAL RESULTS = WIDESPREAD CORRUPTION (2026-06-11, machine rebooted mid-scan):** sweep of all 88 UPLOAD_READY files interrupted at 82/88; visible results (53-82, saved to `מאסטר\sweep-partial-2026-06-11.txt`): **שיעור_05** files 08/10/11 broken (865-1200s black each), **שיעור_06** 7-of-8 broken (up to 1473s=24.5min black), **שיעור_07** all clean, **שיעור_08** 3-of-4 broken, **שיעור_09** files 03+04 broken. Results for files 1-52 lost to a tail-buffer (only last 30 lines flushed) — **rerun needed**: `py "מאסטר\scripts\sweep_black_scan.py"` (saved there; fix it to write incrementally + tee to file, not buffered tail). After full map → rebuild broken files from raw sources per the lesson-1 recipe (proven: clean raw copied LOCAL → trim per `master_LXX_editor.py` PARTS → re-upload → swap IDs in `get-master-lessons` + course-library.html). Lesson-1 rebuilt file archived at `UPLOAD_READY\שיעור_01\01 - ... (תוקן I3qjx4Xi62s).mp4`. (b) `%TEMP%\master_check\` ~2GB intermediates can be cleaned (rebuild archived to OneDrive). **RULE (hindsight.md): video QA = `blackdetect=d=2:pix_th=0.10` + signalstats YAVG spot-checks; never encode reading from OneDrive — copy local first.**)

> Last updated: 2026-06-10 (21:20) by Claude Code (**MASTER PURCHASE FUNNEL — FULL E2E VERIFIED + 2 REAL BUGS FIXED + DEPLOYED (bot `6dcc52a` → Fly, portal `c1df7f1` → Vercel, both live-verified).** Hillel: "לוודא שכל המשפך של המאסטר עובד". **VERIFIED LIVE:** a REAL purchase already flowed 2026-06-09 — דוד שוקרון, tx 250864170, auto-provisioned + welcome WhatsApp sent same minute (paid 1,500₪ — Hillel: intentional for David specifically; everyone else 1,900). Content gate proven with a temp paid user: `get-master-lessons` → 12 modules + 6 technique modules (anon 401). Hillel + team DID reply to David humanly (sendByApi=false msgs through 10.5; his post-purchase msgs answered off this line per Hillel). **BUG 1 🔴 (the welcome link was broken):** magic-link redirect used `#master` → GoTrue appends its own `#access_token` fragment → double-hash breaks session detection. FIX: `PORTAL_MASTER_URL` (cardcom.js + paid-customers.js, incl. manual-activation welcome) → `course-library-v2.html?view=master`; v2 boot honors `?view=master` ONCE at boot (not in handleHash — popstate safe), strips the param, fails closed via setCourse's paid guard. **BUG 2:** parser didn't know Cardcom's real field names (learned from the 06-09 raw payload): amount=`suminfull`/`ProdPrice`, email=`UserEmail`, phone+=`InvMobile` — sum no longer "—" in owner alerts, buyer-email matching now works. **DATA FIX:** David's sub end_date 2027→2099 lifetime (live DB). **E2E PROOF (headless Playwright vs LIVE site, temp paid user, deleted after):** opened the actual magic link → lands logged-in, activeCourse=nlp-master, pill "NLP מאסטר", 12 modules rendered, 0 JS errors, token+param stripped from URL. **GOTCHAS:** raw REST `admin/generate_link` needs TOP-LEVEL `redirect_to` (nested `options.redirect_to` silently ignored → SITE_URL fallback; supabase-js maps correctly — almost misdiagnosed); Green API human phone replies have `sendByApi=false` — fetch deep history before claiming "unanswered". **STILL OPEN:** Green API 7103515939 expires 12.6 (TOMORROW+1); (b)/(d)/(e) from the 17:25 entry unchanged.)

> Last updated: 2026-06-10 (17:25) by Claude Code (**ADMIN→PORTAL ROUTING + CARDCOM DEEP-PROBE + BOT LIFETIME ALIGNMENT — all deployed & live-verified.** (1) **Admin login routing (`5ec0aed`, Vercel, live-verified):** Google/email login sent admin to the CRM (`admin.html`) with NO way back. Fixed: login.html+login-v2.html admin → `course-library-v2.html`; gold "לפורטל הקורסים" button in CRM header; admin-only CRM icon `#hdAdminLink` in portal header (shown via `checkPaidRole`); free-portal(+v2) early-redirect lets admin/paid_customer through WITHOUT the lead questionnaire; legal-gate + auth-guard `redirectByRole` aligned (admin home = portal). (2) **Cardcom pipeline deep-probe (Hillel: "תסדוק לעומק"):** VERIFIED LIVE — Cardcom page 200, `MASTER_CHECKOUT_URL` on 3 buttons + sticky bar, webhook fail-closed 403 on Fly, `CARDCOM_WEBHOOK_TOKEN`+`PARTNER_PHONE` secrets deployed, bot WhatsApp instance 7103533485 `authorized`. **CLOSED the 17:00 entry's OPEN (a):** crm-bot aligned to lifetime (`3502d2a`, deployed to Fly + pushed to private repo) — cardcom.js auto-provision `months:12`→lifetime (2099 sentinel, same as admin-paid.js), bot "פתח גישה" default = lifetime (explicit months honored), templates show "לכל החיים ∞" via new `isLifetime()`; bot price default was already 1900. **Auto-expire cron predicate VERIFIED SAFE** (`lt(end_date, now)`; warnings use exact-day windows — 2099 never matches). RISK: bot instance 7103533485 free-tier w/ quota history — buyer welcome may 466 (owner-alert fallback exists). (3) **Ram hero video renamed on YouTube** (new util `update-video-meta.js` in youtube-uploader skill): "סרטון מבוא · קורס NLP MASTER" → "איך להפוך למומחה NLP · תוכנית המאסטר עם רם אלוס" — oEmbed-verified, privacy confirmed UNLISTED (closes that check for this video; rest of master library still unverified). **STILL OPEN (from 17:00 entry):** (b) one-time data fix for EXISTING paid customers' end_dates → 2099 — ⚠️ business decision needed: old 8,880₪ annual customers may legitimately be 12-month; (c) Green API 7103515939 expires 12.6; (d) browser-verify popup + admin lifetime UI; (e) pre-existing dirty tree untouched.)

> Last updated: 2026-06-10 (17:00) by Claude Code (**FULL QA (scale-turbo, 7 audit agents) + FIX WAVE (8 agents) — 8 atomic commits pushed `3ced127`→`e53390a`, Vercel deploying.** Audit verdict: core healthy (gating sound end-to-end, no secrets, game syntax clean, 4/4 past security findings fixed) + 8 serious findings, ALL FIXED same session: (1) **Admin pricing**: every activation path created 8,880₪/12mo subs → now 1,900₪ + lifetime (end_date sentinel `2099-01-01` — schema has NOT NULL, cron predicate lives in crm-bot repo so far-future is the safe model; UI shows "לכל החיים"/∞; quickActivate too). (2) **training_cta upsell popup wired into LIVE v2 portal** (was registered only in retired v1 → never fired; now: modal + config-driven trigger lesson_complete≥5, free_user only + local paid/admin guard, logs to popup_events, CTA→goToMasterCheckout→Cardcom; also `checkPaidRole` now feeds real role to `PopupManager.setUser`). (3) **docs/master-course/ (10 paid-course transcripts) was PUBLIC** → blocked in .vercelignore + vercel.json rewrites, surgical (whitelist kept: contract-text.html, booklets/ — runtime-fetched). Also blocked meeting-*.md, sales-rep-guide, ux-audit-report, design-system, content-leverage, nano-banana-prompts, questionnaire-therapist-recruitment. (4) **master-practice.html: 5 PLACEHOLDER clips filtered from render** (44 live, dynamic counts, auto-reappear when real IDs pasted). (5) **Admin stored-XSS: 51 innerHTML sinks escaped** (leads/patients/instagram/export; added escAttr/escJsStr for attr+onclick contexts since escapeHtml doesn't cover quotes). (6) **Admin wiring**: sales_rep auto-switch → `switchView('funnel')` (old code queried nonexistent 'sales' nav → blank screen); learning group stacking fixed (leads/learners added to VIEW_GROUPS + header); admin-auth fail-closed try/catch. (7) **Game: 31 exercises' wrongExplanations fixed** (m6 null-at-wrong-index, m4 empty arrays, m1/m2 missing distractor feedback, m3/m4 oversized trimmed; validated null-at-correct-index + no business jargon across all 7 modules). (8) **Backup repair**: `_archive_subscriptions/_archive_signed_contracts` removed from table list (renamed back in June restore → caused PARTIAL), `subscriptions`/`signed_contracts` added; connectivity preflight + 3×60s retry on URLError (root cause of 9.6+10.6 FAILs = DNS dead at 07:00 wake); manual backup run kicked off. **PRODUCT FACT (Hillel 10.6):** single paid product — Master plan 1,900₪ (anchor 6,200) = Master course + workshops + Practitioner techniques. NO 2,600₪ bundle (Ram-meeting bundle idea dead). **OPEN:** (a) crm-bot repo: bot activation likely still 8,880/12mo + verify auto-expire cron predicate vs 2099 sentinel; (b) existing paid customers in DB with 12-month end_dates need one-time data fix to 2099 or cron revokes them; (c) Green API 7103515939 expires 12.6 (TOMORROW+1) — backup/watchdog WhatsApp alerts + portal bot die with it; (d) browser-verify popup + admin lifetime UI; (e) pre-existing dirty tree (submit-lead rewrite, send-welcome-whatsapp, demo.html, deleted PNGs, 5 untracked migrations) still uncommitted — untouched.)

> Last updated: 2026-06-10 (13:07) by Claude Code (**MOBILE SALES-PAGE POLISH — DEPLOYED `9a2da9e`, verified live.** Driven by 3 phone screenshots Hillel sent mid-session (WhatsApp → Downloads). (1) **Mobile header overlap FIXED GLOBALLY** (≤900px, all portal views): the grid let the course pill render UNDER the icon buttons (hamburger sat on the pill text) → switched `.lms-hd` to flex row, pill truncates with ellipsis, icons 40px — geometric overlap-probe in Playwright confirms no intersection. (2) **Sales-view top compaction** (≤760px, scoped `body.view-master-sales`): amb-bar slimmed to one line (sub hidden), body/main gaps tightened, hero badge/headline/video margins reduced — first mobile screen now shows badge+headline+video+price start (was mostly chrome). (3) **Gallery natural aspect** (≤560px): `aspect-ratio:auto` — the landscape Ram+Tony photo (1600×999) was cropped 4:5 and cut people out; all 3 people now visible. (4) **Quick-tip lightbulb FAB deleted entirely** (markup+CSS+`setupFab`+call) — it overlapped the sticky buy-bar price and used `alert()`. Verified via local Playwright iPhone viewport (390×844, `?dev=1` works on localhost) + live curl markers. **GOTCHA:** PS5.1 here-string commit messages break on embedded double quotes (git got half the message as pathspec) — keep commit messages quote-free.)

> Last updated: 2026-06-10 (14:15) by Claude Code (**AUDIT CLOSURE ROUND — DEPLOYED `1f1b62f`, all verified live.** (1) **TECHNIQUES_MODULES moved server-side**: `get-master-lessons` now returns `{modules, techniques}` (same paid_customer/admin gate); the 9 technique YT IDs deleted from `course-library-v2.html` (live-verified: only `let TECHNIQUES_MODULES = [];` remains, populated via the existing `fetchMasterModules()`). **Zero paid YouTube IDs remain anywhere in public page source.** (2) **`get-lessons` hardened**: auth gate (Bearer token → getUser, 401 otherwise) + `Cache-Control: private, no-store`; zero callers in repo so nothing breaks. Both functions deployed, both live-verified 401 on anon. (3) **GitHub Pages auto-deploy DISABLED** (`deploy.yml` → workflow_dispatch only; verified `www.therapist-home.com` serves `Server: Vercel`, and the new push triggered NO Pages run). Pages site itself 308-redirects to the domain. Committed together with the pre-existing exclusion-hardening edits to deploy.yml. (4) **`git gc --prune=now`**: 5,458 loose objects (522MB) → 0 loose / single 450MB pack — OneDrive corruption risk gone. **STILL OPEN (not security-blocking):** browser-verify master+techniques load for a REAL paid user; confirm Master/technique YT videos are unlisted-not-public on YouTube; commit the 5 untracked migrations + submit-lead rewrite (pre-existing dirty tree, untouched); optional `gh api` removal of the stale Pages deployment.)

> Last updated: 2026-06-10 (13:30) by Claude Code (**SECURITY AUDIT FIXES (scale-turbo, 6+3 agents) — DEPLOYED `df450d7`, all verified live.** Phase 1+2 audit (grade C+) then executed the fix wave. **(1) 🔴 Master paywall moved SERVER-SIDE:** new Edge Function `get-master-lessons` (deployed) returns the 12-module/96-video MASTER_MODULES payload only to authenticated `paid_customer`/`admin` — mirrors the production-proven `ai-chat` pattern (getUser + profiles.role, CORS allow-list). `course-library-v2.html` no longer carries ANY master YouTube ID in source (live-verified: `Rab2Wzcon34`/`KryXIuiM57Y` absent); data populates via `fetchMasterModules()` AFTER `checkPaidRole()`, before `renderAll()`, fails closed. **NOT yet verified with a real paid login in browser** — admin convention password didn't work against this project's auth; ANON path live-verified 401. **(2) `?dev=1` bypass now localhost-only** (single compute site, line ~1796) — local preview workflow preserved, prod bypass dead. **(3) Asset diet:** learning-booklets now serves `nlp-booklet-clean.pdf` (2.3MB, was 43.8MB branded); `.vercelignore` += `/primer.md`, `/hindsight.md` (live-verified 404), `תמונות קטלוג לאתר`+`-small`, `סרטוני AI`, `מסירה לעיצוב פורטל`, `demos/`, `demo-portal/` (~93MB unreferenced). **(4) Config consolidation:** patient-step4/therapist-step4/questionnaire-form hardcoded URLs → `window.SUPABASE_CONFIG`; js/ modules were ALREADY config-first (audit finding stale). **(5) pre-push hook (local, not versioned)** runs `scripts/validate-games.js`, blocks push on errors — ran live on this push (623 exercises, 0 errors). **(6) `.gitignore` += meta-ads/, carousels/, _quarantine/, supabase/.temp/ (untracked cli-latest).** **Commit was SELECTIVE** — pre-existing dirty tree (submit-lead 387-line rewrite, 5 untracked migrations, deploy.yml, deleted PNGs) left untouched, still uncommitted. **OPEN:** (a) `TECHNIQUES_MODULES` (~10 paid practitioner-demo YT IDs, line ~3432) still hardcoded client-side — same leak class, move to the same Edge Function; (b) verify master view loads for a REAL paid user in browser; (c) `get-lessons` fn has NO auth (audit High); (d) GitHub Pages workflow ignores `.vercelignore` (audit Critical — dual-deploy mismatch); (e) commit the 5 untracked migrations + submit-lead rewrite; (f) `git gc` (537MB loose objects in OneDrive); (g) ensure Master YT videos are UNLISTED-not-public regardless.)

> Last updated: 2026-06-10 (10:54) by Claude Code (**MASTER-SALES CONVERSION POLISH + "questionnaire submit error" DEEP-DIAGNOSIS & HARDENING — 2 commits LIVE: `9adaab4`, `1113001`.** **(1) Master sales page (`course-library-v2.html`, `9adaab4`)** — acted on Hillel's design feedback. Root-fixed the width collapse robustly: `.lms-master-sales` got explicit `width:100%; max-width:820px; margin-inline:auto` (+`body.view-master-sales .lms-main{width:100%}`) — a definite width independent of any flex/grid quirk, **scoped to the sales view so other portal views are untouched**. Conversion polish: gold discount badge ("חיסכון 4,800 ₪ · 70% הנחה"), bigger now-price (3.1rem) + struck old price, reassurance row under the CTA (מאובטח/מיידי/אחריות), **consent checkbox moved BELOW the CTA as subtle text** (was a heavy box above it cutting the flow — JS gate still finds it via `.lms-ms-price` closest; kept id + `data-master-consent`), **sticky mobile buy-bar** (`.lms-ms-stickybar`, ≤760px, replaces the generic `.lms-bnav` in this view; `#masterStickyCta` scrolls to the offer so it never trips the consent gate on an off-screen checkbox), more vertical rhythm, `<bdi>`-wrapped Latin terms (MASTER/PRACTITIONER/NLP/AI). NOTE: the "מה לומדים" 11-item section was ALREADY a card grid (`.lms-ms-grid`) — it only *looked* like a wall of text because the width had collapsed; the width fix restores 3-up. Verified live. **(2) "כשמנסים להירשם ולמלא את השאלון יש שגיאה" (`1113001`)** — Hillel pinned it to **questionnaire submit**. Proved the **backend is 100% healthy**: reproduced the full submit in a headless Playwright Chrome as a real logged-in user (temp user via admin API → session injected into `localStorage['sb-<ref>-auth-token']` → drove the 3-step form on the LIVE page) — **SUCCEEDS end-to-end** in both variants (the page hides `#phone-field` when the profile already has a phone). Real submissions are landing in the DB. So the error is **client-side, AFTER the row is saved**: `submitForm` wrapped the INSERT + all post-save analytics/`showSuccess` in ONE `try`, so any post-save throw surfaced the generic "אירעה שגיאה בשליחת השאלון" even though the data saved. **FIX:** wrapped `showSuccess()` (on failure → mark done + redirect to portal, never a false error) and `trackFormSubmission()`; now ONLY a genuine insert/session failure shows the alert. **Adjacent bug fixed:** registration (`free-portal.html`) profile upsert sent a non-existent `roles` column → PostgREST **PGRST204** → the WHOLE upsert failed silently, dropping UTM attribution on every signup. Removed it (role is set by the `handle_new_user` trigger); verified the fixed upsert now succeeds and `utm_source` saves. Both deployed + verified live. **GOTCHAS LOGGED** (hindsight): REST default schema is NOT `public` (→`graphql_public`); supabase-js sends `Accept-Profile:public` so the app is fine, raw curl/fetch must add it · deleting an auth.users row 500s while a `profiles` row exists (`profiles.id`→auth.users has no `ON DELETE CASCADE` — delete the profile first) · `prevent_double_submit()` uses `IF FOUND` after `EXECUTE 'SELECT…'` which never sets FOUND → that duplicate-guard is a silent no-op. **OPEN:** could NOT reproduce Hillel's EXACT error (generic flow works for me) — asked him to retry post-fix and, if still failing, give the console `Submit error:` line or the test email so I can replay that exact account.)

> Last updated: 2026-06-10 (10:52) by Claude Code (**ELAZAR REMOVED from welcome-WhatsApp automation, DEPLOYED.** הלל: כבר לא עובדים עם אלעזר, והודעת הברוכים-הבאים האוטומטית שכל נרשם לפורטל מקבל עדיין הזכירה אותו. הוסר שמו מהשורה על הזומים החודשיים בתבנית `WELCOME_TEMPLATE` של `supabase/functions/send-welcome-whatsapp/index.ts` (שורה ~83) — נשארה כללית: "• זומים חודשיים עם מטפל מקצועי שעובד עם המתודה..." (בלי שם מחליף, רם לא הומצא). פרסתי מחדש `npx supabase functions deploy send-welcome-whatsapp --no-verify-jwt` (project `eimcudmlfjlyxjyrdcgc`) — חי. **OPEN:** אלעזר עדיין מוזכר ב-~33 קבצים אחרים (pages/training-v2/3/4.html, ai-chat fn + knowledge-master.ts, nlp-game-data-master m1/2/3/6/8/9, carousels/brand/avatars.json, docs/business+marketing) — לא נוקו, ממתין להחלטת הלל אם לנקות את הציבוריים. שינוי קוד בלבד בקובץ הפונקציה; לא נגעתי ב-git/דפים אחרים.)

> Last updated: 2026-06-10 (08:40) by Claude Code (**PAGE-SPEED FIX — image bloat, DEPLOYED `42c8adb`.** Hillel reported the site loading very slowly. Root cause = multi-MB PNGs: free-portal landing loaded a single **8MB** `free-portal-hero.png`; total loaded images ≈ **15.4MB**. Converted the 6 actually-loaded heroes/photos to **WebP** (Pillow, q80-86, downscaled oversized dims: hero→1920w, portrait→900w) → **554KB (96% smaller)**; free-portal-hero 8MB→124KB, hero-bg 2.1MB→185KB. One `sed -E` pass swapped all `.png`→`.webp` refs across html/css/js (index, free-portal, free-portal-v2, course-library, project-lobby, about, both nlp-game pages, nlp-game.js); bumped `sw.js` CACHE_NAME v1→v2. **Kept original PNGs as rollback** (now unreferenced ⇒ zero perf cost). Deleted 2 genuinely-unused 6.8MB PNGs (`hillel-photo.png` [page uses the 49KB .jpg], `explainer-thumbnail.png` [0 refs]) = 13.6MB off the repo. Verified LIVE: webp serves 200 (124/185KB), deleted png 404, home HTML references .webp not .png. Earlier same morning: `9adaab4` (parallel session) fixed the master-sales mobile width-collapse — confirmed live + WebKit-rendered. **OPEN (secondary, not done):** optimize logo PNGs (101KB+51KB), subset FontAwesome (153KB full font), defer GA/FB analytics (~310KB); remove the 6 rollback PNGs after a few days live. Lesson in `hindsight.md`.)

> Last updated: 2026-06-09 (21:46) by Claude Code (**LAUNCH-READINESS SWEEP for the 1,900₪ Master + FULL DEPLOY, multiple commits incl. `5951d5d`.** scale-turbo audit (7 agents) then fixed every launch blocker, all LIVE and verified. (1) **master-practice.html gate**: now grants access by `profiles.role IN (paid_customer,admin)` — was `course_access`-table only (never populated) so EVERY paying customer was blocked. (2) **Mobile**: root-fixed horizontal scroll (`overflow-x:clip` on html/body, NOT hidden since header is sticky) + 44px touch targets + collapse `#masterTab/#techniquesTab` into hamburger at ≤900px. Verified via Claude Chrome on local auth-neutered test copies (portal redirect-to-prod blocks localhost login). (3) **Contract to 1,900₪ digital**: rewrote `docs/contract-text.html` + `pages/sign-contract.html`, new PUBLIC `pages/master-terms.html`; acceptance = required checkbox gating master checkout (not hand-signature, per contract's "purchase=acceptance"). Filled Ram license `206655367` (🚩 confirm current). (4) **WhatsApp welcome THROTTLE** (anti spam-ban): new `welcome_queue` table (`sql/welcome_queue.sql`, manual-apply) + `enqueue-welcome` + `welcome-queue-processor` Edge Functions + pg_cron every minute (`sql/welcome_queue_cron.sql`, ~35/min drain); 5 signup call-sites rewired from direct send to enqueue. (5) **Cardcom gap closed**: added terms URL (master-terms) to the Cardcom payment page (page 6, UID `oLgmreU4...`) via Claude Chrome, so a mandatory terms checkbox now shows at the payment moment too. (6) **Hero title fix**: generic `.lms-master-section h2` (0,1,1) silently overrode `.lms-ms-hero__h` (0,1,0) so the sales title was right-hugging + 1.2rem; raised the hero rules to `.lms-master-section h2.lms-ms-hero__h` (centered + 2rem/1.5rem). (7) Quarantined old 8,800₪ contract to `_quarantine/2026-06-09/`. **Deploy-order law**: SQL+functions FIRST, frontend push LAST (else gap-window signups miss welcome). **OPEN**: confirm Ram license number; optional add the consent checkbox to the master-practice CTA too. Design polish done in a parallel session (`f6894c7`).)

> Last updated: 2026-06-09 (evening) by Claude Code (**PREMIUM DESIGN PASS — portal headings + summaries dark-glass, DEPLOYED `f6894c7`.** Pure visual/typography upgrade toward the 1,900₪ Master feel — **zero functionality touched, design layer only**. **(1) Portal `course-library-v2.html`:** added `:root` var `--font-display:'Frank Ruhl Libre',...` and applied it to the big headings (`.lms-hero__title`, `.lms-ann__title h2`, `.lms-ann-featured__title`, `.lms-pl-bar__h`, `.lms-partners__title`, `.lms-amb-bar__h`) — FRL was imported but barely used → "generic SaaS" feel; **removed ALL negative `letter-spacing` on Hebrew** (17 selectors, hurt readability); raised every sub-11px font to a readable floor; **gold restraint** — decorative solid-gold labels on dark (`.lms-ms-badge`, `.lms-journey__level`, `.lms-sb-item__badge`) → transparent tint + gold border + gold text (gold is now an ACCENT; kept as fill only on real CTAs/icons/earned-states); `left:6px`→`inset-inline-start` on `.lms-master-card__duration`. **(2) Summaries-master (8 pages):** root problem was a flat-white cheap-template look vs the dark-glass portal. Built ONE shared stylesheet **`css/summaries-master.css`** (premium dark-glass: glassmorphism cards, FRL headings, gold-accent, logical RTL, line-height 1.65, AA contrast) that drives ALL 8 pages → single source of truth. Per-page transform (script `scripts/restyle_summaries.py`): big inline `<style>` → `<link>` to shared CSS, Frank Ruhl added to font import, legacy inline `.site-credit` style removed, homework `🎁` emoji replaced by a CSS gold-diamond marker, `border-right`→`border-inline-end`. **Body content byte-for-byte untouched.** Originals backed up to `pages/summaries-master/_backup/` (local, NOT committed). **(3) Push hygiene:** committed ONLY the 9 design files — repo had dozens of unrelated uncommitted changes from other sessions, all left untouched. **Verified LIVE** on therapist-home.com (CSS `Premium Dark-Glass` marker + `--font-display` present, not stale cache). **Preview method for gated pages:** summaries use `paid-gate.js` (fails closed) → built gate-stripped copies in `%TEMP%\sm_preview` (outside deploy tree, never in repo) + local http.server to view; course-library-v2 has a built-in `?dev=1` auth bypass. Utility scripts left LOCAL (not committed). **OPEN/next:** summaries `data-theme` toggle is now moot (always-dark by default); structural a11y (skip-link/focus-trap) is a separate track, not done here.)

> Last updated: 2026-06-09 (19:31) by Claude Code (**MASTER SALES PAGE LIVE + OPENED TO ALL + HERO VIDEO + PRACTITIONER TECHNIQUES — deployed `1474886`→`52b7e58`→`4c2f85a`→`cf3591b`.** Coordinated 5 parallel Claude sessions editing `course-library-v2.html` as the **single pusher/integrator** (only this session touched git; others edited disjoint regions, signalled done). (1) **#master-sales opened to ALL portal members** (was admin-only): sidebar item, `#master-sales` hash route, `showMasterSalesView` guard, subtitle copy — the SALES page is marketing; CONTENT stays gated. (2) **Hero**: embedded Ram intro video (unlisted YouTube `Kktx3L-3uqs`) + 4 Ram photos (portrait in letter + 3-image journey gallery) + Chrome-QA fix for broken hero h2 (`.lms-master-section h2{display:flex}` wrecked the long title → scoped flex to icon titles, hero is `block`) + orange CTA + mobile polish. (3) **Practitioner Techniques** — new gated catalog: 9 unlisted-YT clips in 6 modules (`TECHNIQUES_MODULES`), `techniquesTab` pill (default `display:none`, shown only to paid via `checkPaidRole`), `setCourse('nlp-techniques')` gated on `_isPaidCustomer`, separate progress key `completedLessons_techniques`. (4) **iOS Safari fix**: `vercel.json` 307 redirect `course-library.html`→v2 (avoids JS `location.replace` back-button bug) + `profile.html` resume links straight to v2. **CONTENT-GATING AUDIT:** master/techniques/workshops gated CLIENT-SIDE only (tabs hidden + `setCourse`/`showMasterView` fail-closed) — BUT unlisted YouTube IDs sit in page source (View Source leak). **3 follow-ups in memory** ([[beit-vmetaplim-future-architecture-debt-note]]): hermetic content lock (Edge Function = Level 1, recommended); deploy WhatsApp welcome (personal Ram wording ready in crm-bot `paid-customers.js`, NOT deployed); welcome-email decision (Cardcom receipt vs branded email). Also: Ram now gets an auto WhatsApp on every Cardcom sale (crm-bot deployed).)

> Last updated: 2026-06-08 (09:30) by Claude Code (**VIDEO-PLAYER + SAVE-PROGRESS MARATHON — all on live `course-library-v2.html`, deployed `61a6744`→`501ed7e`.** **🔴 Root video bug:** `YT.Player('plIframe')` does NOT inject the embed src into an `<iframe>` already on `about:blank` → black screen (Claude Chrome confirmed: `src=about:blank`, zero `/embed` requests, `YT.Player`=true, thumbnail 200). **Fix:** `loadLessonVideo` now ALWAYS calls `_fallbackIframe` (direct `iframe.src=youtube-nocookie/embed`). **Reverted my own facade** (poster+▶) + API warm-up — they're what left the iframe on about:blank. Memory: [[project_beit_vmetaplim_video_player_direct_embed]] — do NOT re-try facade/YT.Player. **🟢 Save-progress fixed:** `markLessonCompleted` no longer BLOCKS on the `lesson_completion_events` (analytics) write — writes both it + `course_progress`, one success = saved. **Console cleaned:** 406 (`.single()`→`.maybeSingle()` on user_notes/course_progress/last_watched), 403 `lead_attribution` (skip INSERT for authenticated — table has only "service role only" policy; row already exists from signup so PATCH handles it), 622 `postMessage` spam (gone once facade about:blank removed). **🟢 Cardcom paid-flow LIVE end-to-end:** `CARDCOM_WEBHOOK_TOKEN` set on Fly (`crm-bot-hillel`, webhook verified 200 w/ key), Indicator URL pasted into Ram's Cardcom dashboard ("האם לבצע דיווח עסקה נוסף" field on the קורס מאסטר payment page), `MASTER_CHECKOUT_URL` wired to Ram's Cardcom link, sales view renamed "דף מכירה"→"תוכנית המאסטרים שלנו". Page-speed: supabase-js+config moved out of `<head>`, Font Awesome `preload`, YouTube `preconnect`. **🎬 Lesson-1 edit IN PROGRESS (not built yet):** opening has 2 confusing covers (dark luxury 0-2.7s + petrol 2.8-4.1s) → keep DARK only; Ram enters mid-sentence ("קצת טכניקות מעולה", 110s) → full opening sentence is at **98s in the raw source** `סרטונים-מלאים/מפגש 1 - מאסטר.mp4` (raw is **720p**, deliverable is 1080p → quality step). 2 WhatsApp previews sent for Hillel approval. **OPEN:** (a) Hillel approves preview + picks cover + decides 720p-vs-upscale → then build full episode + re-upload; (b) optional: clean the lone `lead_attribution` 403 via RLS, move video to Bunny/Cloudflare for sub-2.6s load.)

> Last updated: 2026-06-07 (later) by Claude Code (**PAID-FLOW REPAIR + GATED SUMMARIES — pushed `6e7a76a`.** Triggered by Hillel verifying Ram's admin access + reviewing what the 1,900₪ Master product has/lacks. **(1) 🔴 CRITICAL DB RESTORE (live, via Management API):** the May cleanup had archived the two paid-core tables — `subscriptions`→`_archive_subscriptions`, `signed_contracts`→`_archive_signed_contracts` (both 404 in public). Both archives were EMPTY + kept all RLS/policies/FKs/constraints, so restored with a plain `ALTER TABLE ... RENAME` back + `NOTIFY pgrst`. Verified 200 via REST. The 4 paid_customers were unaffected (access is via `profiles.role`, not these tables). **(2) CTA WIRING:** both Master "buy" buttons in `course-library-v2.html` now route through ONE constant `MASTER_CHECKOUT_URL` (currently '' → graceful "coming soon" toast; paste Ram's Cardcom URL there + push = done). Removed the dead `onclick="return false"`; handled in `setupLessonNav` delegation. **(3) GATED SUMMARIES:** new `js/paid-gate.js` (fails-closed paid_customer/admin gate, hides page until verified) injected into all 8 `pages/summaries-master/*.html`; removed `summaries-master/` from `.vercelignore` (now protected, no longer publicly exposed). **NOT browser-verified yet — built blind per Hillel, verify gate in Claude Chrome next.** **DISCOVERED ALREADY-DONE (memory was stale):** purchase automation IS built+deployed — `crm-bot/src/services/cardcom.js` + `server.js` route `/api/cardcom-webhook?key=<CARDCOM_WEBHOOK_TOKEN>` (matches buyer→opens subscription+paid_customer→magic link→WhatsApp→owner alert, idempotent); price is ALREADY 1900 everywhere (not 8880). **OPEN for Hillel+Ram:** (a) Cardcom payment-page URL → MASTER_CHECKOUT_URL; (b) set the webhook URL in Ram's Cardcom; (c) verify CARDCOM_WEBHOOK_TOKEN set on Fly; (d) browser-verify summaries gate. **PARKED TODO (Hillel, next session):** adapt the registration CONTRACT (`הסכם הרשמה לתוכנית הכשרה\הסכם הרשמה.txt` + `pages/sign-contract.html`) from the old 8,800₪ annual program to the 1,900₪ digital course — 5 decisions pending, see memory `project_beit_vmetaplim_master_contract_adaptation`. sign-contract is currently an orphan (linked nowhere); new flow has no contract step.)

> Last updated: 2026-06-07 (10:01) by Claude Code (**PREMIUM-UX MARATHON + COST GATING + SUPABASE CLEANUP — all on the LIVE portal `course-library-v2.html`, deployed.** (1) **Communication-Styles workshop** added to the paid Master area as module 12: a single YouTube video `KryXIuiM57Y` split into 8 timestamp chapters (chapters carry `start` seconds; extended both `playMasterLesson` and `selectLesson` to honor `&start`). (2) **Bonus separation** — flagged modules 11+12 `isBonus`; they now render in a distinct gold "🎁 בונוסים בלעדיים" group (sidebar heading + `#masterBonusSection` grid) instead of looking like core sessions (Hillel: a bonus that looks like a lesson cheapens its value). (3) **5 premium features built** (from web research): `#journeyPanel` "המסע שלך" (completion ring + XP + levels[500/lvl] + badges, derived from completedLessons), completion **celebrations** (confetti + WebAudio chime + milestone modal), **resume** mid-video via YouTube IFrame API + localStorage position (safe fallback to manual iframe), AI mentor **memory** (learner-context preamble: progress + streak + this lesson's notes). (4) **COST GATING — "whatever costs money → Master only":** the AI mentor (only per-call-cost feature) is now paid-only — locked teaser in `resetAiChat` + `sendAi` guard (client) AND a server-side `role` check in the `ai-chat` Edge Function (deployed `--no-verify-jwt`; 403 for non paid_customer/admin). Free features stay open to all. (5) **Supabase health audit + cleanup:** root spam = `crm_activity_log` (176K rows / 52MB, ~2,600/day) = 98% bot heartbeat logs. Deleted 137,721 old tick rows (kept all real actions + last 14d), dropped dead `lead_attribution_dedup_backup_20260520`, VACUUM FULL → log 52MB→11MB, DB 218MB→172MB. **Root fix in the CRM bot:** `src/scheduler/cron.js` `instrumented()` now skips logging routine SUCCESS for 3 heartbeats (`smart_automations_tick`/`new_lead_polling`/`pipeline_callback_reminder`); errors still logged. Deployed to Fly (`crm-bot-hillel`). Other audit findings clean (RLS on 50/52, 0 dup emails, no stuck queues; minor: 192 dup phones in lead_intake, empty `course_access` has RLS off). **OPEN:** Cartcom URL for master-sales CTAs + Ram intro video still pending; Hillel's Supabase access token still active (not revoked). Commits `33b6422`→`86c27a6`.)

> Last updated: 2026-06-06 (22:34) by Claude Code (**MASTER SALES PAGE added INSIDE the live portal — admin-only preview, DEPLOYED `9c34b56`.** Imported the Master landing-page copy (Hillel's `דף נחיתה קורס מאסטר.txt`) as a styled in-portal section below "תוכנית השותפים", visible **only to role=admin** for now. **CRITICAL ROUTING LESSON:** first did all the work in `pages/course-library.html` + verified via curl it was live — but Hillel saw nothing. Claude Chrome inspected the live DOM and found different IDs (`sbMasterItem`, `window.Auth`). **`pages/course-library.html` is RETIRED — line ~32 does `location.replace('course-library-v2.html')` immediately. The LIVE portal is `course-library-v2.html`.** Reimplemented everything in v2: new `#masterSalesView` section as a `body.view-master-sales` view (matches the dark-glass theme), `sbMasterSalesItem` sidebar entry, `showMasterSalesView()` + `#master-sales` hash route + nav-click handler, gated by `_isAdmin` (set in v2's `checkPaidRole()` when `profile.role==='admin'`). Reverted the dead edits in the old file. Verified live via curl. **OPEN (non-blocking):** (a) CTA buttons `data-cta="master-checkout"` have no Cartcom URL yet (currently `onclick="return false"`); (b) Ram intro video is a placeholder; (c) DECISION: when launching to everyone, change the gate from `role==='admin'` to a permanent show. Verified Hillel's profile is role=admin via service-key REST query (3 admins total: htjewelry / hilellaknine / alus2132).)

> Last updated: 2026-06-04 (13:37) by Claude Code (**NLP MASTER GAME HARDENED — DEPLOYED, commit `c624bc7` pushed to master.** Audit (3 Explore agents) of the paid Master game + shared engine, then 5 fixes via /plan. Shared engine `js/nlp-game.js` powers BOTH the free `nlp-game.html` (7 modules, live users) and the paid `nlp-game-master.html` (10 modules, `window.GAME_COURSE='master'`) — all changes kept backward-compatible; Master-specific behavior is behind a flag, free game untouched. **(1) 🔴 Paid-access gate** — `nlp-game-master.html` now sets `window.GAME_REQUIRE_PAID=true`; new `verifyPaidAccess()` in `onAuthSuccess` queries `profiles.role`, **fails CLOSED** (guest/missing/error → deny), denies → `lockOutPaidContent()` toast + redirect to `course-library.html`. **Closed the hole where any logged-in free user could play full Master content via the URL** (was only hidden, not gated). **(2) 🔴 Crash resilience** — guard in `checkIdentifyAnswer()` for malformed `correctRange` (was destructuring → crashed whole lesson); new `validateModules()` runs once at startup, shows a friendly error screen instead of a silent crash if a data file failed to parse. **(3) 🟡 Reliability** — 6s `Promise.race` timeout on `loadPlayerData()` (loading screen could hang forever); fixed leaderboard bug `best_streak: playerData.longestStreak` (was reading nonexistent `bestStreak` → every player saved streak 0); `match` exercise now releases a pair when you click a matched item (was a dead-end on misclick). **(4) 🔵 a11y/brand** — loaded `accessibility.css` + Frank Ruhl Libre (welcome-gate title only, UI stays Heebo), ESC closes overlays (`initKeyboardDismiss`), ARIA (`role=dialog`/`aria-modal` on modal, `role=status aria-live` on feedback, `role=group` on options-list, `aria-expanded` + Enter/Space on Ram FAB, `aria-label` on sound toggle), RTL-correct zigzag (`padding-inline-*`). **(5) 🔵 onboarding** — one-time first-exercise tip toast for brand-new players (`maybeShowFirstExerciseTip`, localStorage-gated per course). Files: `pages/nlp-game-master.html`, `pages/nlp-game.html`, `js/nlp-game.js`, `js/nlp-game-leaderboard.js`, `css/nlp-game.css`. `node --check` clean on both JS files. **Pushed ONLY these 5 files** — repo had many unrelated uncommitted changes (course-library.html, deploy.yml, migrations, etc.) left untouched. **OPEN (non-blocking):** (a) browser QA not done — terminal can't render; verify match + RTL + gate in Claude Chrome on the live site; (b) lockout redirects to `course-library.html` with no "locked" message there — optional small add. Hillel said "תדחוף" — explicit push approval.)

> Last updated: 2026-06-01 (15:35) by Claude Code (STANDING CONTEXT recorded — created `docs/funnel_and_tech_debt_context.md`, the official "Future Architecture & Debt Note" (`.vercelignore`-blocked + mirrored to Claude memory). Encodes 3 STANDING items: (1) ⛔ **DB TECH-DEBT FREEZE** — do NOT fix/refactor/repair/`db pull`/`migration repair` migrations (lead_intake DRAFT-but-applied landmine, `_archive_*` tables, history divergence) until explicitly told; apply idempotent SQL manually meanwhile. (2) **Master funnel = 3 segments**: old graduates (win-back) / active free-learners (in-portal popup — built) / instant-OTO on thank-you page (not built). (3) **Future deep student-tracking** (questionnaire free-text + AI-agent interaction *content*; `ai_chat_usage` logs counts/tokens only today) — build only when asked. No code/DB changes this segment — documentation only.)

> Last updated: 2026-06-01 (afternoon) by Claude Code (MASTER UPSELL FUNNEL + readiness — executes parts of today's Ram-meeting plan (items #1/#3/#4). **Staged, NOT pushed — awaiting Hillel OK.** (1) Re-enabled the dormant `training_cta` popup in `course-library.html`: a `free_user` who finished ≥5 lessons → modal → CTA navigates to Ram's Cartcom page `https://secure.cardcom.solutions/EA/EA5/oLgmreU4vkf2bf73tIxpA/PaymentSP`. Built `showTrainingCta()`/`goToMasterCheckout()` + re-activated the register block (was commented out 2026-05-24). DB: `popup_configs` upsert via migration `20260601150000_master_upsell_popup.sql` (status=live, audience=free_user, trigger=lesson_complete, min_lessons=5) — APPLIED by Hillel in SQL editor. (2) **Positioning PIVOTED mid-build** per Hillel to PERSONAL-DEVELOPMENT (not "become a therapist"): popup headline "מוכן להעמיק את ההתפתחות האישית שלך?". (3) **FIXED broken welcome link** in crm-bot `src/templates/paid-customers.js` — it pointed to the deleted `learning-master.html` (= 404 for every paying customer on activation) → now `course-library.html#master`. (4) **Added `#master` deep-link** in course-library.html (`_pendingMasterOpen` flag → `switchToMaster()` after `checkPaidRole` loads master content) so the welcome link drops the customer straight inside Master. **Readiness audit:** Master content = only מפגש 1 (8 chapters) live; sessions 2-10 pending Ram's re-upload (per today's meeting). **OPEN / LANDMINES:** (a) ⚠️ PRICE MISMATCH — crm-bot `subscription-service.js` hardcodes price `8880` but the new Master price is **1,900₪**; fix before first real sale. (b) `summaries-master` still `.vercelignore`-blocked → paid users 404 on "סיכומים"; re-enable w/ auth-guard (~15 min). (c) verify `subscriptions`/`signed_contracts` weren't archived to `_archive_*` in the May cleanup (bot still writes to `subscriptions`). (d) DECISION PENDING: show full 10-session skeleton w/ "בהכנה" placeholders vs only-ready content. NO push/deploy made this session — staged only.)

> Last updated: 2026-06-01 by Claude Code (STRATEGY MEETING with Ram — full record in `docs/meeting-ram-2026-06-01.md`. **Major direction shift: Master course sales RE-ACTIVATED** (reverses the 2026-05-29 refocus that hid the Master funnel). New model: sell Master at **1,900₪ anchored to 6,200₪ = 70% off**; Practitioner stays free forever; **bundle** Practitioner+Master lifetime **2,600₪**; up to 5 installments; certificate+exam DEFERRED. Go-to-market: start with the **existing/internal audience** (WhatsApp groups, the 21-day group), paid+tests later. Launch framing = "limited-time / price may rise" (NOT a hard 72h, to avoid looking salesy). **Hillel builds:** (1) Cartcom (קארטקום) sales page — copy already approved; (2) a Master info page INSIDE the portal (course-library) explaining the content deeply — START HERE for existing audience; (3) purchase automation: Cartcom webhook → auto-open `paid_customer` role + welcome WhatsApp; (4) portal layout = popup + top button + side button + affiliate program below, all → Master page; (5) proactive WhatsApp/story outreach to Practitioner viewers, esp. 10+ lessons done. **Ram does:** refresh/re-upload all courses + send the missing Practitioner techniques table. Positioning angle (open): "top 5% who control their mind" + testimonials of admired people at Master level. Ram's key note: first buyers buy regardless of copy (warm audience) — ship, don't over-polish. Numbers: 714 portal registrants (check how many are reachable with the offer), 20% keep streak, ~0.5–1% conv → ~2 customers from base initially, ~400 new names/month. **Code impact:** this partially reverses the 29/05 `.vercelignore` hiding of `learning-master.html`/`training.html`/`master.html`/`summaries-master/` — the new Master page is built inside the portal, but verify whether any of the old pages need re-enabling before building. NO code changes made this session — meeting documentation only.)

> Last updated: 2026-06-01 by Claude Code (CLEANUP AUDIT — read-only parallel Workflow, 39 agents, 7 clusters + adversarial verify, over 1,971 files → **153 cleanup candidates** in 3 risk packages. Executed **Package 1 only** (Hillel: "קודם חבילה 1 בלבד"). 29 safe items (69MB: broken `${comp}.mp4`, 7 root screenshots, `mobile-audit/`, empty `ux-audit-2026-02-19/`, 4 regenerable script dumps, `verify_phase2_attribution.py`, `assets/logo-game.jpg`, 2 orphan docs, 10 empty FAILED backup folders + 1 partial) **MOVED (not deleted) to `_quarantine\2026-06-01\package1-cleanup\`** — fully reversible, has MANIFEST. Quarantine sits under already-ignored `/_quarantine/` → zero deploy exposure. NOT committed/pushed — local only. **Verified:** all critical live files intact (index, supabase-client/config, nlp-game, course-library); 20 real backup snapshots + 30 ZIPs untouched. **AWAITING Hillel:** Package 3 (archive ~2.4GB redundant uncompressed backup folders + `temp_audio/` — biggest ROI, safest) and/or Package 2 (orphan code: `js/admin-dashboard.js` legacy 1734-line monolith [0 HTML loads it], dead FlutterFlow set [`flutterflow-api/` + `get-lessons` fn + `supabase/types/lessons.ts`], 9 one-shot scripts, 8 superseded sql/ bootstrap files, duplicate design copies in `docs/design-system/`+`demos/lms-glass/`). Permanent deletion of quarantine ONLY on explicit OK. CONFIRMED-NOT-CLUTTER: `supabase-client.js` vs `supabase-config.js` are complementary, not duplicates. RE-FLAGGED LANDMINE: `supabase/migrations/20260518080000_create_lead_intake.sql` marked DRAFT but already applied manually = the thing blocking `db push`.)

> Last updated: 2026-05-29 by Claude Code (Strategic refocus DEPLOYED — 3 commits pushed to master `9c6bb19`, `4e2c2fa`, `8052b54`. Site pivoted from dual "social-initiative + school" to **school + free NLP course only**. Unpublished (via `.vercelignore`, files KEPT — reversible per Hillel) the entire patient/social side (`project-lobby`, `landing-patient`, `patient-onboarding/dashboard/intro`, `patient-step1-4`, `join-patient`, `registration`→patient-step1, `questionnaire`), therapist recruitment (`landing-therapist`, `therapist-onboarding/dashboard`, `therapist-step1-4`, `join-therapist`), `demo.html`, `training-v2/3/4`, and `learning-master.html` (the ₪8,880 Master sales page). Nav cut to 3 items (דף הבית · אודות · פורטל הקורסים) across all live pages; עלינו→אודות, ספריית הקורסים→פורטל הקורסים; project-lobby links removed from all navs+footers; footer tagline → "בית ספר מקצועי להכשרת מטפלים מוסמכים בשיטת NLP". Rewrote copy on index.html (hero/about/CTA/meta/JSON-LD, deleted social pillar, added 3-card "what you get" grid: course/materials/AI-mentor+game), about.html (founder letter reframed to school+free course, kept personal voice, softened Ram's free-therapy-for-fighters line), privacy-policy.html (2 מיזם clauses removed), and legal-gate.html (rewrote terms from patient↔therapist matching to learning-platform terms — removed 10-sessions, free-therapy-for-veterans, therapeutic-confidentiality). sitemap.xml + robots.txt cleaned. NOT done: visual browser QA (code-only verification); legal-gate terms should get a lawyer review. Out of scope/untouched: admin dashboard (still references מיזם internally — kept for archived-data/future relaunch), paid Master masterView area.)

> Last updated: 2026-05-24 10:53 by Claude Code (Two commits today, BOTH deployed. (1) `927f382` — bootstrapped PopupManager + marketing-tools.js into `course-library-v2.html` + added 2 engagement popups: `top_percentile_v2` (fires when user hits 2nd lesson of current ISO week → "top 15%" message) and `weekly_streak_v2` (fires on auth when 2+ consecutive ISO weeks visited → adaptive copy). Both use `.lms-toast` + localStorage tracking, static thresholds (no RPC — db push still blocked by `lead_intake` landmine). SQL inserts in `sql/insert_engagement_popups.sql`, applied via SQL editor. (2) `5d79e35` — hid the MASTER training marketing funnel. `.vercelignore` blocks `pages/training.html`, `pages/master.html`, `pages/summaries-master/` (3 paths — note that last one is a temporary breakage for paid customers in masterView — they'll see broken "סיכומים" links until summaries-master gets re-enabled with proper auth-guard, ETA next session ~15 min). Removed ~25 CTAs across 10 public pages (incl. the entire Pillar-2 card on index.html). `training_cta` popup disabled in v1 code; companion SQL `UPDATE popup_configs SET is_active=false, status='archived' WHERE popup_id='training_cta'` applied via SQL editor. Ported 6 popups (auth_modal + auth_wall sharing one DOM, ai_questionnaire 7-field form → course_questionnaires, video_toast, share_prompt with WhatsApp+copy, referral_toast) from v1 to v2 in fresh `lms-modal`/`lms-toast` style. `showLmsToast()` refactored to read `data-popup-id` from element instead of hardcoded 2-popup map. STILL OPEN from prior session: `referral_leaderboard_exclude_staff` migration awaiting confirmation it took effect.)

## Last task completed (2026-05-24, 10:53): 2 motivation popups for v2 + hide MASTER training marketing funnel — BOTH DEPLOYED
- **Two commits shipped.** `927f382` adds the engagement popups + bootstraps popup infra in v2 (which had zero PopupManager / marketing-tools.js — discovered during 20/05 audit). `5d79e35` removes the public Master-training marketing funnel and ports 6 v1 popups to v2.
- **Popup #1 — `top_percentile_v2`.** Fires when `markLessonCompleted()` records the user's 2nd lesson completion of the current ISO week. Copy: "השלמת 2 שיעורים השבוע! הנתון הזה מכניס אותך ל-15% העליונים של הלומדים הפעילים בפורטל." Static threshold (no RPC — see lead_intake landmine below). Hook at line ~1855 of `course-library-v2.html`. `.lms-toast` bottom-left, auto-hide 8s, max 1/day, 60min cooldown vs streak.
- **Popup #2 — `weekly_streak_v2`.** Fires inside `authGate()` after a successful session check. `consecutiveWeekStreak()` walks back through `weeksVisited` array in localStorage. If ≥2, fires popup; copy adapts ("השני", "השלישי", "ה-N"). Hook at line ~1275 of `course-library-v2.html`. Same toast style.
- **Tracking.** All in `localStorage('lms_engagement_v1')` JSON: `{weeksVisited: ['2026-W21', ...], lessonsByWeek: {'2026-W21': 3, ...}}`. ISO week computed from browser local time. Pruned to last 12 weeks visits / last 4 weeks lesson counts.
- **PopupManager bootstrap.** Added `marketing-tools.js` + `popup-manager.js` script tags before `supabase-client.js` (line 1165 area). Also added 5 lifecycle popup divs + 6 fresh modal/toast registrations later in the file (Section "popups port" below).
- **Files touched in commit 927f382.** `pages/course-library-v2.html` (+157 lines), `sql/insert_engagement_popups.sql` (new — keep under `sql/` NOT `supabase/migrations/`, db push blocked).
- **SQL applied via Chrome ext → Supabase SQL editor.** 2 rows in `popup_configs` (popup_id, title, message, category=engagement, priority=3, is_active=true, status=live, max_per_day=1, cooldown_minutes=60, target_audience=authenticated, trigger_event=manual). Verified.
- **Master training funnel hidden.** Hillel asked "hide the training program page". Audit revealed his mental model was off: `learning-master.html` doesn't exist; real public entry points are `pages/training.html` (live, indexed) + `pages/master.html` (noindex but URL-accessible). Bonus discovery: `pages/summaries-master/` (7 paid-content pages) had NO auth-guard — public URLs. Hillel approved blocking all 3 via `.vercelignore` knowing summaries-master breakage would temporarily hit paid customers inside masterView.
- **25 CTAs removed across 10 public pages.** index.html (Pillar 2 card deleted entirely + nav + hero + bottom CTA + footer), about, courses, free-portal (+v2), project-lobby, questionnaire, learning-booklets (dead link to learning-master.html), legal-gate (bonus). v1 course-library: header CTA deleted + `trainingCtaOverlay` div deleted + `PopupManager.register('training_cta', ...)` commented out + show/hide functions emptied to stubs.
- **DB cleanup.** `UPDATE popup_configs SET is_active=false, status='archived' WHERE popup_id='training_cta'` applied via SQL editor.
- **6 popups ported v1→v2 in fresh lms- style** (NOT copy-paste — full visual redesign in v2's design language): `auth_modal` + `auth_wall` (shared `#lmsAuthModal`, dynamic copy via data attrs, `_lmsActiveAuthPopup` mutex), `ai_questionnaire` (7-field form → `course_questionnaires`, `localStorage('questionnaireCompleted')` gates re-show), `video_toast` (60s timer in `selectLesson` for unauth guests, "להירשם" inline CTA), `share_prompt` (fires from `markLessonCompleted` when `completedLessons.length >= 3` + 2s delay, WhatsApp + copy-link buttons, uses `window.Referrals.getLink`), `referral_toast` (info-level lms-toast, register-only). **SKIPPED `training_cta`** (sales funnel killed).
- **Critical refactor in v2.** `showLmsToast()` previously had hardcoded mapping `elId === 'lmsToastTopPercentile' ? 'top_percentile_v2' : 'weekly_streak_v2'` for `PopupManager.dismiss()` — would dispatch wrong dismissals for the 5 new toasts. Sonnet agent refactored to read `data-popup-id` attribute from element. All 5 toast divs got `data-popup-id="..."`.
- **New `.lms-modal` pattern added to v2 CSS.** Centered modal overlay + card, matches v2's design tokens (`--bg-surface`, `--accent-gold`, `--border-subtle`, `--radius-lg`). Used `[hidden]` attribute pattern (not `.is-active` class) to match toast convention.
- **What was NOT tested (be honest).** Zero manual browser verification of any popup. Trust = code review of all 3 anchor points (script tags + 2 hooks) + agent reports. Risk areas: auth_modal Google OAuth flow in v2 (might not work without `supabaseClient.auth.signInWithOAuth` config), questionnaire submit to `course_questionnaires` table (RLS unclear), mobile layout of `.lms-modal` overlay.
- **What's open for next session.**
  1. **summaries-master re-enable with auth-guard** — paid customers currently can't access summaries from inside masterView (URLs return 404). ~15 min fix: add `auth-guard.js` to the 7 summaries pages + remove from `.vercelignore`. Hillel knows.
  2. **Browser smoke-test of 6 ported popups** in production (Chrome ext or his own eyes).
  3. **Referral leaderboard staff-exclusion** — still doesn't show in `referral_leaderboard` view per 20/05 session. Re-confirm.
  4. **(If MASTER program restarts)** revert `.vercelignore` 3 lines + restore 25 CTAs (use commit `5d79e35` as base — most CTAs are recoverable via revert).

## Last task completed (2026-05-20, 13:33): Referral leaderboard staff-exclusion + popup system audit — BOTH AWAITING HILLEL

### Task 1 — Remove "Ram Alus" from the referral/ambassador leaderboard
- **Trigger.** Hillel: the ambassador leaderboard (`referral_leaderboard` view, shown in the portal's ambassador tab) has a #1 referrer with 9 referrals while everyone else has 1-2 — makes other ambassadors feel they have "no chance". Asked to remove him.
- **Investigation (service-key REST query).** #1 = "Ram Alus" (`c70537e4-a15b-440f-a8bf-7ff6ff28ff9a`, alus2132@gmail.com, **role=admin**, 9 referrals, all within 30d). He has 3 duplicate accounts (alus21@ / alus213@ / alus2132@gmail.com — same person; the other two are role student / student_lead with 0 referrals). The leaderboard account is the admin one.
- **Decision.** Exclude ALL admin-role accounts from the view, not just Ram by ID — structural fix, future-proofs against Hillel/Elazar/any staff polluting the board. His leaderboard account IS the admin one, so this removes him.
- **Built.** Migration `supabase/migrations/20260520120000_referral_leaderboard_exclude_staff.sql` — `CREATE OR REPLACE VIEW referral_leaderboard` with added `AND p.role IS DISTINCT FROM 'admin'`. Identical column list to migration 049, so CREATE OR REPLACE is safe and preserves grants + security_invoker.
- **Apply path.** Gave Hillel the SQL to run in the Supabase SQL editor — NOT `db push` (see landmine below). First run: Hillel said "רץ" but verification (service-key query of the live view) showed Ram still #1 — the view definition did NOT change. Re-issued a self-verifying combined snippet (CREATE OR REPLACE + a SELECT of the leaderboard so he sees the result inline). **AWAITING his re-run + confirmation.**
- **After the fix lands:** #1 becomes "מעין עינבר שוע" with 2.
- **Verify with:** service-key query `referral_leaderboard?select=*` — confirm `c70537e4...` absent. Throwaway inspection script at `C:\Users\saraa\find-ram-elos.py` (delete after Task 1 confirmed).

### Task 2 — Popup system verification (Hillel: "check popups work, emphasis mobile")
- **Headline finding.** The new primary portal `pages/course-library-v2.html` contains **ZERO** popup code — `grep -i 'popup|modal'` → 0 matches, no `popup-manager.js` script tag. It also does NOT load `marketing-tools.js` (only 3 scripts: supabase CDN, supabase-config, supabase-client). So on v2: no popups, no cookie consent banner, no Meta Pixel, no GA4, no UTM capture.
- **Old `course-library.html`** DOES load `marketing-tools.js` + `popup-manager.js` and has 7 `PopupManager.register` calls — full system.
- **Split routing.** `free-portal.html` redirects logged-in + questionnaire-done users → `course-library-v2.html` (no popups). Homepage navbar "ספריית הקורסים" → `pages/course-library.html` (old, has popups). Users get popups or not depending on entry path.
- **`popup_configs` live:** 6 popups all `status=live, is_active=true` (auth_modal, auth_wall, cookie_consent, share_prompt, training_cta, video_toast) — none can fire on v2.
- **Status.** Reported to Hillel in plain Hebrew. Asked whether to port the popup system + marketing-tools.js into v2 (all 6 popups, or start with critical: auth + cookie), then point the navbar to v2. **AWAITING his decision.** Porting is a build task — v2 has a different DOM/UI ("lms-" classes); the old popup show()/hide() functions cannot be copy-pasted.

### Repo landmine discovered — `db push` is unsafe right now
- `lead_intake` table **exists in the live DB** (confirmed via REST probe) but its migration file `supabase/migrations/20260518080000_create_lead_intake.sql` is headed "DRAFT — NOT FOR EXECUTION YET" and uses bare `CREATE TABLE public.lead_intake (` (no `IF NOT EXISTS`). It was applied to live by other means (dashboard SQL editor) and is almost certainly NOT recorded in remote `schema_migrations`.
- Therefore `npx supabase db push` would treat it as pending, run `CREATE TABLE` → error "relation already exists" → whole push fails. **Until this is reconciled, apply ALL schema changes via the Supabase SQL editor, not `db push`.**

## Last task completed (2026-05-18, 13:47): Local outreach-tool for Master training launch (NOT IN GIT)
- **What it is.** Standalone Node.js+Express server at `outreach-tool/` (gitignored) — Hillel runs it locally via `run.bat` → opens browser at `http://localhost:3010`. Lets him manually outreach to Master training leads via WhatsApp with full UI: 4 heat tiers (חם בוער/חם/פושר/קר), per-row send w/ message editing, mark "פניתי"/"לא רלוונטי", notes per lead (synced to Supabase `caller_notes`/`sales_notes`), history view, light/dark theme, auto opt-out detection.
- **Why not an admin tab.** Originally planned as admin dashboard tab + Edge Function + `outreach_log` migration. Hillel pivoted mid-session ("פרטי שלא יעל לגיט"). All planned project files were rolled back; tool rebuilt as standalone, then relocated INTO the project folder but gitignored. Pattern logged at `obsidian-vault/Insights/preferences.md` ("פרטי שלא יעל לגיט").
- **Architecture.**
  - Code: `outreach-tool/` (server.js, public/, package.json) — synced to OneDrive (visible in Explorer), gitignored + vercelignored.
  - Secrets + logs: `C:\Users\saraa\.outreach-data\` (NOT in OneDrive, NOT in git). `.env` + `logs/outreach.jsonl|marks.json|notes.json|opt_outs.jsonl`. `server.js` reads from `process.env.DATA_DIR || os.homedir() + '/.outreach-data'`.
  - WhatsApp: Green API instance `7103515939` (Business $12/mo, paid plan, valid until 2026-06-12). The free instance `7103533485` (crm-bot) had quota exhausted.
  - Data source: directly queries `profiles`+`portal_questionnaires`+`contact_requests`+`course_progress` with service-role key (mirrors `admin_get_all_leads()` RPC logic in JS since RPC requires admin JWT which service-role lacks).
- **Enforced rules.** No sends 20:00–08:00 Israel time. 14-day dedup per phone. Opt-out line auto-appended. `/api/mark` syncs status to source row in Supabase (`status='potential'/'rejected'`, `sales_stage='contacted'/'not_relevant'`). Opt-out poller hits Green API `receiveNotification` every 60s and auto-marks anyone replying "הסר"/"stop"/"unsubscribe"/"סטופ".
- **Verified live.** 20 sends today (14 hot_burning + 6 hot). 2 early failures with old instance (HTTP 466). Opt-out poller live but no replies yet — first reply will validate the @c.us-vs-@lid phone matching logic in `handleNotification()`.
- **Touched in beit-vmetaplim repo (will commit to git):**
  - `.gitignore` +1 line: `/outreach-tool/`
  - `.vercelignore` +2 lines: `/outreach-tool/` block
  - `primer.md` (this file) — new entry
  - Nothing else. The tool itself is gitignored.
- **Files in `outreach-tool/` (NOT committed):** `server.js`, `package.json`, `package-lock.json`, `node_modules/`, `public/{index.html,app.js,styles.css}`, `run.bat`, `README.md`, `.gitignore`, `.env.example`.
- **How to relaunch from a fresh session.** Open `beit-vmetaplim/outreach-tool/run.bat` (double-click) OR PowerShell `Start-Process -FilePath "node" -ArgumentList "server.js" -WorkingDirectory "C:\Users\saraa\OneDrive\שולחן העבודה\beit-vmetaplim\outreach-tool" -WindowStyle Hidden`. Background `node server.js` via Bash wrapper is unstable on this Windows machine — use PowerShell `Start-Process` with `-WindowStyle Hidden -PassThru`.

---

## Last task completed (2026-05-17, 13:53): Supabase deep cleanup — stage 1 done, stage 2 designed (awaiting 24h observation)
- **Trigger.** Hillel asked for thorough Supabase reorg after 3 business pivots left 35 tables with overlap and orphans. `/plan` mode, multi-stage approach.
- **Stage 0 — backup.** Adopted today's 07:00 auto-backup as pre-cleanup baseline (35 tables, 127k rows, 68MB). Snapshot of 119 migration files. Copy at `C:\Users\saraa\backups\supabase\beit-vmetaplim\20260517-pre-cleanup\`. **Discovered backup gap:** `scripts/backup-supabase.py` only covered 1/7 buckets (`workbooks`); 6 others (`contracts`, `therapist-documents`, `community-images`, `legal-docs`, `automation-assets`, `ig-publishing`) had zero backup. Also `nlp_game_players` (10 rows of game save state) was missing from TABLES list. Both fixed in same commit.
- **Stage 1 — archive 16 tables (DONE).** Migration `20260517100000_archive_dead_tables.sql` applied to live + pushed to git. 16 RENAMEd to `_archive_*` (`questionnaire_submissions`, `matches`, `appointments`, `subscriptions`, `signed_contracts`, `ad_campaigns`, `crm_notes`, `crm_payments`, all 5 `community_*`, `patients`, `therapists`, `sales_leads`). 3-gate verification all green: old names → HTTP 404, `_archive_*` names → exact previous row counts, active baselines unchanged. Rollback ready at `sql/rollback/20260517100000_archive_dead_tables.rollback.sql`.
- **Adjacent regression discovered.** Edge Function `submit-lead` whitelist still allows the 4 archived table names (`patients`, `therapists`, `sales_leads`, `questionnaire_submissions`) and 7 frontend call sites still send to them (`registration.js` x3, `patient-flow.js`, `patient-step4.js`, `therapist-step4.js`, `community.js` x3). Practical risk = low (zero traffic in 3+ weeks) but non-zero. Hillel chose Option B: solve via stage 2 design instead of quick patch.
- **Stage 2 — designed + dry-run (NOT EXECUTED).** 5 design files staged under `docs/specs/lead-intake-v1/`:
  - `00-overview.md` — full design rationale in business Hebrew (problem, solution, 7 technical principles, user_id matching strategy, rollout order, risks)
  - `01-schema.sql` — unified `lead_intake` table (36 cols in 7 groups, 9 indexes incl. GIN on JSONB payload + partial unique for dedup, RLS with admin-full / user-own-row, 5-value intake_type CHECK, updated_at trigger, VIEWs for admin backward-compat in commented section)
  - `02-submit-lead-v2.ts` — Edge Function rewrite that accepts BOTH legacy `{table, data}` AND new `{intake_type, core, payload}` shapes; auto-translates legacy → new, **which also fixes the regression above without any frontend changes**
  - `03-backfill.py` — idempotent migration of 5 source tables → `lead_intake`, with email/phone → `profiles.id` matching for user_id linkage, dry-run by default, batched inserts of 100
  - `04-frontend-diffs.md` — before/after for 11 frontend call sites (cleanup, not blocking)
- **Dry-run result (validated against live).** 646/646 rows transformable. user_id match rate: portal_questionnaires 459/459 (100%), contact_requests 174/182 (95.6%), `_archive_*` 5/5 (100%). 8 anonymous contact_requests leads → user_id NULL (not lost, anonymous by design).
- **Decision: 24h observation before stage 2 apply.** Hillel explicitly chose to wait — same person who in 2026-05-09 did 5 rapid deploys in 6h on MindSet. New pattern logged: schema/DB-level changes get observation gates even when all tests green. See `obsidian-vault/Insights/work-style.md` (Risk-tier patience).
- **What to watch in 24h.** Tomorrow's 07:00 auto-backup will validate the updated config (36 tables + 7 buckets, success = no WhatsApp). If anything breaks in live (site/admin/bot) — Hillel triggers rollback from `sql/rollback/20260517100000_archive_dead_tables.rollback.sql`.
- **Files created/modified.**
  - `scripts/backup-supabase.py` — STORAGE_BUCKETS 1→7, TABLES updated to use `_archive_` prefix + added `nlp_game_players`
  - `supabase/migrations/20260517100000_archive_dead_tables.sql` (applied)
  - `sql/rollback/20260517100000_archive_dead_tables.rollback.sql`
  - `docs/specs/lead-intake-v1/{00-overview.md, 01-schema.sql, 02-submit-lead-v2.ts, 03-backfill.py, 04-frontend-diffs.md}` (staged, NOT executed)
- **Commits.** `b6e049f` chore(db): archive 18 dead/legacy tables + expand backup coverage.
- **Next available actions (when Hillel says "מתחיל שלב 2").**
  1. Apply `01-schema.sql` (2 sec) → `py docs/specs/lead-intake-v1/03-backfill.py --apply` (~30 sec) → deploy `02-submit-lead-v2.ts` as `submit-lead` (~20 sec) → re-run backfill to catch gap-window inserts (~10 sec). Total live time ~1 min, zero downtime.
  2. **After 1 week stability** — migrate admin dashboard reads from `portal_questionnaires`/`contact_requests` to `lead_intake` (uncomment VIEWs in `01-schema.sql` for zero-touch transition).
  3. **After admin migration** — rename `portal_questionnaires` + `contact_requests` to `_archive_*`. End state: 35 → 17 active + 18 archived. Drop the legacy translation block from Edge Function.
  4. **(Future, separate migration)** — clean up `profiles.role` CHECK constraint to drop `patient`/`therapist`/`sales_rep` values (after verifying no live rows carry them).

---

## Last task completed (2026-05-16, 21:53): Welcome WhatsApp automation verification (read-only)
- **Trigger.** Hillel asked to verify the welcome WhatsApp automation works end-to-end, with emphasis on the silent-group invite (`chat.whatsapp.com/Lp3MNZ7fmGu8dmUzaUluyx`). `/plan` mode.
- **Approach decided via AskUserQuestion.** Full verification (code + DB + live send). If a bug is found → document and wait for approval (no autonomous fix).
- **Phase A — code review of 4 call sites.** All 4 verified correctly wired with proper payload (`profile_id` or `questionnaire_id`), fire-and-forget pattern, `keepalive: true` on the 3 that navigate. `WELCOME_TEMPLATE` clean (Elazar, no em-dashes, opt-out line "ענה הסר"). CORS includes `www.therapist-home.com`. Rollout cutoffs (`2026-05-11T21:00Z` for questionnaire, `2026-05-13T00:00Z` for profile) both safely in the past.
- **Phase B — Supabase DB queries (read-only).** Last 48h: 22/22 profiles with phone marked sent · 18/18 questionnaires marked sent · 0 stale rows · today 10/10. Green API canary state file: `ok` at 17:57.
- **Phase C — live test send.** Direct `curl` on already-welcomed Hillel profile returned `{"sent":false,"reason":"already_sent"}` (idempotency proved). Then NULL'd `whatsapp_welcome_sent_at` + bumped `created_at` past rollout cutoff on `fd8cb9d8-929c-47e1-9cac-d007e627ca26` (htjewelry email), POSTed to Edge Function, got `{"sent":true}`, new timestamp `2026-05-16T18:40:17.949Z` set in DB. Both columns restored in `finally` block. Hillel confirmed WhatsApp message arrived.
- **Scaling follow-up — Hillel asked about 100 new users.** Volume queries: 62 actual sends this month (39 profile + 23 questionnaire) post-rollout, 29/24h current rate. Green API `getStateInstance` = `authorized`, `delaySendMessagesMilliseconds: 1000` (1msg/sec rate ceiling — 100 users drain in ~100 sec, not a failure). Quota count not exposed via API; canary at 09:00 daily is the only monitor (worst-case 24h detection window, but missed messages retry on next ensureProfile call — no permanent loss).
- **Pre-rollout trap discovered.** When testing live, `pre_rollout` reason is returned for profiles older than `PROFILE_ROLLOUT_CUTOFF`. The cutoff check is on `profiles.created_at`, not on the NULL state of `whatsapp_welcome_sent_at`. To force an actual send on an old profile, both columns must be temporarily bumped. Pattern: temp DB mutation → always `try/finally` with restore.
- **Files touched.** None in the repo. Three throwaway scripts in `C:\Users\saraa\` (`verify-welcome-automation.py`, `find-hillel-profile.py`, `live-test-send.py`) were created and deleted at the end of the session.
- **No commits, no deploys, no migrations.**
- **Next available actions (still open from 2026-05-14).**
  1. **Proactive renewal alert** — pending Hillel's monthly Green API renewal date.
  2. **CRM bot rate limiter** — `pLimit(30/min)` in `src/whatsapp.js` before next bulk broadcast.
  3. **Welcome retroactive replay** — deferred but increasingly moot as queued rows organically drain via ensureProfile retries.

---

## Last task completed (2026-05-14 #1, 08:09): Welcome WhatsApp לכל הרשמה + Green API quota canary
- **Originally last-updated marker:** 2026-05-14 08:09 by Claude Code (Built signup welcome WhatsApp automation — every new registration with phone gets the group invite, not just questionnaire completers. Fixed the related Green API quota outage by switching Edge Function secrets to instance `7103515939` after Hillel renewed its subscription. Built daily quota canary (`scripts/check_green_api_quota.py` + Windows Task `BeitVmetaplim-GreenApiCanary` at 09:00) that emails Hillel via Gmail Apps Script when checkWhatsapp starts failing. Sent the one missed welcome from overnight (eliavo241) manually. 3 commits: `130d445`, `145e4b9`, `07bea2e`.)

## Last task completed (2026-05-14 #1, 08:09): Welcome WhatsApp לכל הרשמה + Green API quota canary
- **Feature shipped.** משתמש שנרשם לפורטל עם טלפון (free-portal email+password, login.html/login-v2.html OTP) מקבל מיד הודעת WhatsApp עם הזמנה לקבוצה. לפני זה רק מי שמילא את השאלון אחרי ההרשמה קיבל. Google OAuth בלי טלפון — דילוג מכוון; אם ימלא שאלון מאוחר יותר, המסלול הקיים יתפוס אותו.
- **שינויים שבוצעו.**
  - **Migration `20260513120000_profile_welcome_sent.sql`** — `ALTER TABLE profiles ADD COLUMN whatsapp_welcome_sent_at TIMESTAMPTZ` + partial index + `UPDATE` שמסמן את כל 566 הפרופילים הקיימים כ-`NOW()`. Forward-only rollout.
  - **Edge Function `send-welcome-whatsapp/index.ts` — refactor כפול.** עכשיו מקבל `{profile_id}` או `{questionnaire_id}` באותה פונקציה, מסלול משותף ל-opt-out/normalize/checkWhatsapp/send. Cross-row dedup: כשנקראת דרך השאלון, בודקת קודם אם `profiles.whatsapp_welcome_sent_at` כבר מסומן — אם כן, מסמנת את השאלון ומפסיקה (מונע double-send).
  - **4 frontend hooks fire-and-forget** עם `keepalive: true`: `pages/free-portal.html` אחרי upsert, `pages/login.html` + `pages/login-v2.html` בתוך `savePendingProfile`, `js/supabase-client.js` `ensureProfile` כ-net אוניברסלי.
  - **Secrets updated** — `npx supabase secrets set GREEN_API_INSTANCE=7103515939 GREEN_API_TOKEN=d9a0ab4e...` אחרי שהילל חידש את המנוי על ה-instance הזה. ה-CRM bot ממשיך לעבוד על instance שונה (`7103533485`) עם creds משלו ב-`crm-bot/.env`.
- **Green API canary.** `scripts/check_green_api_quota.py` רץ יומית 09:00 דרך Windows Scheduled Task `BeitVmetaplim-GreenApiCanary` (full python.exe path, 8.3 short path לקובץ, `AllowStartIfOnBatteries + DontStopIfGoingOnBatteries`). פרובינג של checkWhatsapp על `972549116092` (הילל). אם invokeStatus != ok → שולח אימייל ל-`htjewelry.a474@gmail.com` דרך Gmail Apps Script (ערוץ נפרד שלא תלוי ב-Green API). State ב-`scripts/green-api-canary.state.json` (gitignored). Re-alert רק כל 3 ימים אם עדיין שבור; שולח אישור התאוששות כשחוזר. **תיעוד ה-overnight retroactive replay מ-13/05 התייתר** — אנשי השאלון מהיום ההוא יכוסו ע"י קריאות עתידיות של ensureProfile (idempotency של ה-Edge Function עובדת לטובתם).
- **One missed welcome — sent manually.** משתמש 1 נרשם ב-2026-05-13 21:51 (`eliavo241@gmail.com`, 058-541-0877) אבל לא קיבל הודעה כי באותו זמן Green API היה שבור. שלחתי לו ידנית דרך curl ל-Edge Function עם profile_id שלו אחרי שעדכנו את ה-secrets. נסגר.
- **Smoke tests שבוצעו לפני git push** (לפי דרישה מפורשת של הילל "תבדוק עוד בדיקות לוודא שהכל מקצועי"). 11 בדיקות עברו: profile_id מסומן → already_sent, profile_id לא קיים → not_found, regression למסלול השאלון, GET → 405, OPTIONS preflight → CORS תקין, מקור עוין → fallback ל-allowed origin, `.vercelignore` חוסם `supabase/`, 566/0 פרופילים pending vs marked, 0 הרשמות לילה בטבלאות אחרות (patients/therapists/contact_requests).
- **Commits.** `130d445` feat welcome on signup, `145e4b9` fix keepalive, `07bea2e` feat canary monitoring.
- **Next available actions.**
  1. **Proactive renewal alert** — אם הילל יספק תאריך חידוש חודשי של Green API, להוסיף התראה X ימים לפני, לא רק כשנשבר.
  2. **Welcome retroactive replay (deferred from 2026-05-13 #2)** — 7 questionnaires מ-13/05 עם `welcome_sent_at = null` עדיין קיימים. עם ה-Edge Function החדש (cross-row dedup) קריאה שניה תעבוד נכון. סקריפט one-off יוסיף כיסוי.
  3. **CRM bot rate limiter (deferred from 2026-05-13 #2)** — pLimit(30/min) ב-`src/whatsapp.js` כדי למנוע chain ban עתידי.

## Last task completed (2026-05-13 #2, 20:56): Silent phone diagnosis — Green API 24h cooldown
- **Trigger.** Hillel reported "the phone is dead quiet today — 8 people signed up to the portal, I got zero admin alerts and zero welcome messages went out". `/plan` mode investigation.
- **Investigation.** Two parallel Explore agents mapped both notification paths (admin via DB trigger → CRM bot → notifyOwners, and user welcome via portal-questionnaire.html → send-welcome-whatsapp Edge Function). Then 5 read-only checks: (1) Supabase REST query for today's `portal_questionnaires` and `profiles`, (2) Green API `getStateInstance` + `getSettings`, (3) Fly.io `crm-bot-hillel` health endpoint, (4) Fly.io machine status, (5) `notAuthorized` confirmation from CRM bot's own self-report.
- **Findings.** 9 leads total in the relevant window: 7 full questionnaires + 2 signup-only profiles (Haya Maryanovski, שיראל ממן). All profiles have phone populated → the DB trigger fired. All questionnaires have `welcome_sent_at = null` → the Edge Function ran but `checkWhatsapp` returned `QUOTE_EXCEEDED`/`notAuthorized`, so it correctly returned 503 without marking sent. Both systems hit the same Green API instance and both died silently together.
- **Root cause (Hillel volunteered after I presented findings).** Bulk WhatsApp broadcast yesterday → Green API anti-spam triggered 24h cooldown on instance 7103533485. Looks identical to a phone disconnect from outside — same `stateInstance: notAuthorized` response.
- **Decisions.** (1) No fixes in this session — diagnosis-only, per Hillel's request. (2) Don't poll Green API repeatedly while it's in cooldown (each call counts toward the throttle). (3) Don't send any new broadcasts until ~12:00 14/05.
- **Memory written.** `~/.claude/projects/C--Users-saraa/memory/project_beit_vmetaplim_greenapi_throttle.md` — incident record + diagnostic playbook for next time. Two new patterns landed in `obsidian-vault/Insights/patterns.md`: "Silence-as-signal" and "Context revealed after diagnosis".
- **Out of scope.** Migration to a proper WABA provider (Twilio/Wati/360Dialog — $50-200/month) was discussed and deferred. Adding Instagram organic posting via the existing Meta Business Manager App was discussed and deferred (would require App Review extension + Personal→Business account conversion).
- **Next available actions (after cooldown lifts).**
  1. **Welcome replay script** — one-off that iterates `portal_questionnaires WHERE welcome_sent_at IS NULL AND created_at::date >= '2026-05-13'` and POSTs each to the Edge Function. ~30 min build, run once, verify in `welcome_sent_at` updates.
  2. **CRM bot rate limiter** — add `pLimit(30/min)` + 1s sleep between sends in `src/whatsapp.js` to prevent future chain bans. ~1 hour build + deploy.
  3. **Green API state monitor** — `check-green-api-state.py` cron every 30 min, fires SMS/email if `stateInstance != authorized`. Run from remote server `192.168.1.125` to avoid loading the main machine. ~45 min build.
  4. **Stretch: Instagram organic auto-posting** — only if Hillel still wants it after the immediate fixes land. Requires (a) IG account conversion to Business (10 min, his action), (b) App Review extension for `instagram_content_publish` + `pages_manage_posts` (1-7 days wait), (c) Python script that pulls from `reels-factory/` and uploads via Graph API (~4 hours).
- **Bridge option discussed.** Buffer/Later ($15-25/month) as a faster path to scheduled Instagram posting while DIY waits for App Review. Hillel didn't commit either way.

---

## Last task completed (2026-05-13 #1): Instagram bio short link with UTM
- **Trigger.** Hillel asked for a shortened, UTM-tagged link to put in his Instagram bio pointing to the portal signup page.
- **Decisions (via AskUserQuestion).** (1) Destination changed mid-flow from `login.html` to `free-portal.html` — landing page with Pixel + clear value prop, not the bare login form. (2) Short.io with branded domain over generic TinyURL — supports future per-channel links (TikTok, YouTube, WhatsApp broadcasts) on same brand. (3) Instagram + bio + portal-signup as utm_source/medium/campaign.
- **Built.** Short.io account (Google OAuth, free `hilell.s.gy` subdomain) + first link `hilell.s.gy/portal` → `https://www.therapist-home.com/pages/free-portal.html?utm_source=instagram&utm_medium=bio&utm_campaign=portal-signup`. Memory updated at `~/.claude/projects/C--Users-saraa/memory/reference_shortio_account.md` for future link creation.
- **Execution method.** Hillel performed all Short.io UI actions via the Claude Chrome extension using self-contained prompts I prepared — link creation + destination URL update were both done browser-side, not via API. He declined the API key step.
- **Tracking flow.** Existing `marketing-tools.js` captures the UTM into `attribution_last_touch` (refreshed every visit, post 2026-05-11 fix); on form INSERT (registration / contact / patient/therapist intake) the UTM is persisted to the lead row. GA4 dashboard tab in admin shows aggregate `instagram` traffic with 24-48h lag.
- **Out of scope.** iOS in-app browser strips UTM on ~30% of paid traffic — partial fbclid recovery only. CLAUDE.md still has stale "redirect" claim for free-portal.html.
- **Next available actions when needed.** Add channel-specific links (TikTok/YouTube/WhatsApp) on same `hilell.s.gy` domain using same Chrome-extension prompt template.



## Last task completed (2026-05-11 #5): Instagram carousel generation system v1

## Last task completed (2026-05-11 #5): Instagram carousel generation system v1
- **Trigger.** Hillel asked for a carousel system for Beit V'Metaplim — wanted "right and beautiful structure first, then scale". Goal: lead-gen via Instagram carousels.
- **Plan + research.** 3 parallel Explore agents mapped (1) Beit V'Metaplim brand/avatars/voice from docs/business/, (2) 2026 Instagram carousel best-practices via WebSearch (slide count 8-10, DM-keyword CTA 6-9× bio link, sends > saves > dwell), (3) existing infra (nano-banana-poster, frontend-design, html-to-pptx, Meta Ads toolkit). Plan agent designed the architecture. AskUserQuestion locked 3 decisions: free-portal audience for v1, HTML+Puppeteer engine, DM-keyword CTA. Plan file: `C:\Users\saraa\.claude\plans\agile-watching-porcupine.md`.
- **Built (`carousels/` new subdir, NOT yet committed).**
  - `brand/brand-tokens.json` + `pillars.json` + `avatars.json` — 5 colors, 2 fonts, 4 pillars, 3 avatars, contrast pairs incl. forbidden combos (gold-on-aqua fails)
  - `templates/slide.html` + `slide.css` — single template, 3 slide-types (hook/insight/cta), RTL-native, 1080×1350, Frank Ruhl headlines + Heebo body, deep-petrol bg with radial-gradient atmosphere, gold corner flourishes (bottom-right intentionally clear for watermark zone)
  - `schema/carousel.schema.json` — JSON Schema 2020-12 validation, cross-field rules enforced in render.mjs (track=master locks presenter=elazar, total slides 8-10)
  - `render/render.mjs` — Puppeteer driver: navigates to slide.html?slide-data injected via `evaluateOnNewDocument`, waits for `document.fonts.ready` + data-ready signal, screenshots at 1080×1350. Writes `manifest.json` with caption template.
  - `render/preview.mjs` — pptxgenjs builds custom-layout 10"×12.5" PPTX (matches 4:5) with each PNG as full-bleed slide, RTL mode on
  - `content/2026-05-12--first-carousel.json` — showpiece: NLP technique, A1, Ram, free track, 9 slides, hook "5 שניות לפני שאתה אומר משהו שתתחרט עליו"
  - `package.json` (puppeteer 23, ajv 8 with `ajv/dist/2020.js` import for draft 2020-12, pptxgenjs 3.12) + `.gitignore` (excludes node_modules + output) + `README.md` (plain Hebrew) + `מה-זה.txt`
- **Verified.** First run produced 9 PNGs all at exact 1080×1350, ~900KB each. Hook + insight + CTA all render correctly in Hebrew RTL. PPTX preview built successfully on second attempt (first failed because we set `pptx.layout='CUSTOM'` before `defineLayout` — fixed by removing the line).
- **`.vercelignore` updated.** Added `carousels/` so the system isn't deployed to therapist-home.com.
- **Awaiting Hillel.** Visual approval of the 3 sample slides (hook, slide 5, CTA) before any v2 work (cadence calendar, A/B, DM automation, Master tier carousels).



## Last task completed (2026-05-11 #4): WhatsApp reminder opt-in system
- **Migration** `20260511180000` — 2 עמודות חדשות: `portal_questionnaires.whatsapp_reminders_consent` (NULL/TRUE/FALSE) ו-`profiles.whatsapp_opt_out` (BOOLEAN). כבר הועלתה ל-Supabase.
- **Frontend** `pages/portal-questionnaire.html` — מסך ההצלחה עוצר את ה-auto-redirect (היה 4 שניות). מציג בלוק "10% בלבד מסיימים קורס" עם "כן, שלחו לי" (ברירת מחדל) / "לא תודה". לחיצה על "כניסה לפורטל" שומרת consent ב-DB ורק אז מעבירה.
- **CRM Bot** `src/services/automation-engine.js` — buildCandidates מביא `why_nlp + whatsapp_reminders_consent` מ-portal_questionnaires ו-`whatsapp_opt_out` מ-profiles. FIELD_REGISTRY קיבל 3 שדות חדשים. `generatePersonalizedMessage()` קוראת Groq עם context של `vision_one_year + motivation_tip + main_challenge` לכל מועמד. executeRule מדלג על opted-out ומשתמש ב-AI אם `action_config.ai_personalized = true`.
- **CRM Bot** `src/intent.js` + `src/handlers/optout.js` + `src/router.js` — intent חדש `whatsapp_optout` לזיהוי "הסר" מלומדים. Handler מעדכן `profiles.whatsapp_opt_out = TRUE`. "חזרה" מאפס. מחובר לראוטר.
- **Admin** `js/admin/admin-automations.js` — תבנית חדשה "תזכורת שבועית מותאמת אישית (AI)" עם `ai_personalized: true`, cron שלישי 10:00, פילטר `reminders_consent is_true + whatsapp_opt_out is_false`. תוקן `readEditorIntoState()` לשמור `ai_personalized` בעת עריכת rule.
- **טרם deployed** — ממתין לאישור Hillel.

## Last task completed (2026-05-11 #3, 09:25): Popup data-integrity fix
- **Trigger.** Hillel asked to verify popups are working and appearing smartly. 3 parallel Explore agents mapped the system: 8 registered popups, robust admin dashboard, A/B infrastructure unused.
- **9 diagnostic SQL queries** ran via Claude Chrome extension against `popup_events` (30-day window). Results:
  - Only 2 popups actually firing in production: `cookie_consent` (570 impressions) and `share_prompt` (142 impressions, 85% dismiss rate — UX problem).
  - 3 popups DEAD: `auth_wall`, `auth_modal`, `video_toast` — all show 0 impressions but `auth_modal`/`auth_wall` had 52 dismisses each (impossible).
  - `ai_questionnaire`: 153 shown, 0 clicks, 57% dismiss (broken CTA or bad copy).
- **Root cause — two bugs in `pages/course-library.html`.** (1) `closeModal()` lines 8320-8327 called `PopupManager.dismiss('auth_modal')` AND `PopupManager.dismiss('auth_wall')` on every close → double-counted. (2) 6 direct `openModal()`/`openModal(true)` call sites bypassed `PopupManager.request()` → no `shown` events ever logged.
- **Fix (`697089d`).** Added `_activeAuthPopupId` tracking variable. Updated 2 register show() callbacks to set it. Updated `closeModal()` to dismiss only the active popup (sets to null first to prevent re-entry on hide-callback). Wrapped 6 direct openModal call sites with `if (window.PopupManager) request() else openModal()` fallback pattern. Sites: selectLesson paywall (7358), markLessonCompleted paywall (7786), switchTab premium gate (8012), handleResource (8034), handleLockedAction (8047), openAuthModal alias (8116).
- **Impact (verify 24-48h).** shown_30d for auth popups climbs from 0 to real numbers. Dismissed counts halve. Admin dashboard analytics become trustworthy.
- **Out of scope (per Hillel's explicit "א" choice — fix counting first, optimize later).** share_prompt 85% dismiss, ai_questionnaire 0% CTR, dead popups investigation, A/B variant experiments, legacy modals migration.

## Last task completed (2026-05-11 #2): UTM persistence fix end-to-end
- **Trigger.** Morning ads report at 08:00 said "Meta dיווחה 10 lid | 0 attributed in CRM" — Hillel saw a sustained mismatch and said "אסור להשאיר דבר כזה". Investigation revealed Meta IS appending UTM correctly; the leak was 100% client-side in beit-vmetaplim.
- **Root cause.** `marketing-tools.js:200` `window.getUtmData()` read from legacy `localStorage('utm_data')` — written once on first signal, never refreshed. Users who landed organic first and later clicked a Meta ad got their UTM stripped at form INSERT time. Same pattern in 6 separate INSERT paths: patient-step4, therapist-step4, registration (x2), supabase-client.ensureProfile, portal-questionnaire.
- **Secondary leak (patient-flow only).** `js/patient-flow.js:352` `mirrorData` for `contact_requests` was built without UTM fields. Edge Function `submit-lead` did have `backfillUtmFromAttribution()` but the schema needs to exist on the client payload first.
- **Tertiary leak.** `js/patient-flow.js:382` `insertPatientRecord` (authenticated re-register path) did direct Supabase INSERT, bypassing the Edge Function and never capturing UTM.
- **Fix (`6e50367`).**
  - `js/marketing-tools.js` — rewrote `getUtmData()` to read `attribution_last_touch` first (refreshed every visit), fall back to legacy `utm_data` with 30-day TTL. Single change auto-fixes the 6 call sites.
  - `js/patient-flow.js` — added `...utm` spread to `patientData` + `mirrorData` in `insertContactRequest`; added `utm` to `patientData` in `insertPatientRecord`.
  - `pages/portal-questionnaire.html` — comment-only update reflecting the new behavior.
- **Verification path.** Tomorrow's 08:00 daily ads report should show `meta_paid > 0` in the CRM breakdown (was 0 today). The `compute_meta_crm_gap` 3-day watchdog in `daily_health.py` will track recovery — gap was 100% on 5/10, should drop below 50% within 1-3 days as new leads come in via fixed paths.
- **Out-of-scope (acknowledged gaps).** iOS in-app browser still strips UTM on ~30% of Meta-paid traffic — that's a Meta-side limitation, fbclid still passes through and ends up in `lead_attribution` for partial recovery. Cookie-cleared users still lose attribution.

## Prior task completed (2026-05-11 #1): Questionnaire reminder audit + preventive fix
- **Context preserved below — original entry from before the UTM fix:** audit of all automations after a lead complained about a wrong "didn't fill questionnaire" reminder — fixed source templates in `admin-automations.js` to require `role=student_lead`, added 2 SQL helpers under `sql/`, deployed `22329ba`. Audit verified ZERO leads were actually affected — the live rule in production already had the correct filter; the fix is preventive for future rules created from the templates.

## Last task completed (2026-05-11): Questionnaire reminder audit + preventive fix
- **Trigger.** A lead complained to Hillel that she received an automated WhatsApp saying "didn't fill the questionnaire" — but she never had a questionnaire. Concern was the reminder was being mis-targeted.
- **Audit (5 parallel Explore agents).** Mapped the full automation ecosystem: 40+ active cron handlers in CRM bot (not 3 as CLAUDE.md said — those 3 are `subscription_*` only), 2 pg_cron jobs in Supabase (Meta-related), 5 Windows Scheduled Tasks, 8 Edge Functions. Found the suspect templates in `js/admin/admin-automations.js:65-89` — two reminder rules filtered only on `filled_questionnaire = false`, no positive check that the user is actually a portal registrant.
- **Root cause analysis.** Engine's candidate pool comes from `profiles` table only (`crm-bot/src/services/automation-engine.js:117,212`). `role` field comes from `profiles.role`. A `student_lead` is the exact state for "registered to portal, hasn't filled questionnaire" — that's the right positive filter. Adding `role = 'student_lead'` to the filter excludes anyone in `profiles` without that role (admins, paid customers, or `null`/empty for orphan profiles).
- **Fix (`22329ba`).** Added `{ field: 'role', op: '=', value: 'student_lead' }` to both reminder templates in `js/admin/admin-automations.js`. Created 2 SQL helpers under `sql/`: `fix_questionnaire_automation_audience.sql` (idempotent runtime UPDATE on `automation_rules.audience_filter` if the rules exist live) + `affected_leads_questionnaire_bug.sql` (audit query joining `automation_runs` × `profiles` to identify any leads with role ≠ `student_lead` who got the message).
- **Verification — Hillel ran the SQL via Claude Chrome extension.**
  1. Bad rule "מי שלא מילא שאלון פורטל" (daily 11AM) doesn't exist in `automation_rules` at all — never created from template.
  2. Bad rule "תזכורת 30 דק׳ אחרי הרשמה למי שלא מילא שאלון" exists AND already had `role=student_lead` filter (someone fixed it before; not in git history I could find).
  3. 36 sent runs in last 30 days, ALL with `role=student_lead`. **0 AFFECTED.**
- **So why did the lead complain?** Not a targeting bug. Most likely UX: `portal-questionnaire.html` requires authentication; an unauthenticated `student_lead` clicking the link gets the login screen and perceives "no questionnaire". Message text also doesn't identify the sender or registration date — feels like spam.
- **Out of scope (deferred to future sessions, per Hillel's explicit choice "רק הבאג הדחוף").**
  - 40+ CRM bot automations audit — many fire daily/4×daily WhatsApp messages (birthday_greeting, satisfaction_survey, payment_reminders, post_treatment, etc.). Hillel asked for a recommendation table next session.
  - **NightWorkSystem failing every night** — schtasks `NightWorkSystem` (02:00) exits with code 255. Bat file path or PATH issue.
  - **Role escalation in crm-bot** `src/config.js:48,80` — any unknown phone defaults to `role='admin'`. Documented in CLAUDE.md, not fixed.
  - Improve reminder message wording — add "צוות בית המטפלים" sender, registration date, opt-out line.

## Current State
- **Status:** Active. course-library-v2.html is now the primary portal end-to-end: ambassador + master + lesson player + tabs + notes + AI chat all in one file. Free-portal.html and portal-questionnaire.html now redirect logged-in users to v2 (was: old course-library.html). Old course-library.html intentionally retained as fallback for existing bookmarks/email links — slated for decommission in 7-14 days once v2 is verified against real users. Free-portal paid-traffic attribution restored earlier today (Meta Pixel inline in head, implicit-consent model).
- **Last task completed (2026-05-10):** Fixed two dead `learning-master.html` references in `course-library-v2.html` (`e1290b8`).
  - **Symptom.** Hillel reported clicking "פרטים על תוכנית הכשרה" from the v2 portal → page broke. First screenshot was ambiguous (couldn't tell which UI element was the source). Second screenshot included DevTools console open showing `404 Not Found` on `/pages/learning-master.html` — root cause identified.
  - **Root cause.** The file `pages/learning-master.html` was deleted in commit `dd61d29` ("remove unused pages: patient/therapist dashboards, catalog, learning-master") but two references in `course-library-v2.html` were never updated:
    1. Line 785 — header CTA button "תוכנית ההכשרה" (`<a class="lms-hd__cta-2" href="learning-master.html">`).
    2. Line 1978 — `showMasterView()` non-paying-customer redirect (`window.location.href = 'learning-master.html';`).
  - **Fix.** Both replaced with `training.html` — the public training landing page that already exists, has the "השאירו פרטים" form, and was the same target the OLD `course-library.html` used (line 4281 there: `<a href="training.html" class="header-training-cta">`). One commit, 3 lines changed (3 insertions / 3 deletions).
  - **Deploy.** `e1290b8` → master → Vercel. Verified live ~30s after push: production HTML now shows `href="training.html"` instead of `learning-master.html`.
  - **Other dead `learning-master.html` references still in repo (NOT fixed — out of scope for this turn).** Hillel asked only about what he clicked. The remaining occurrences are either passive (sitemap/schema/admin dropdown) or low-traffic:
    - `pages/learning-booklets.html:165` — visible link in nav.
    - `sitemap.xml:88` — Google will get 404 on crawl.
    - `pages/master-practice.html:463` + `pages/summaries-master/index.html:272` — schema.org breadcrumb `BreadcrumbList`.
    - `pages/admin.html:2420` + `pages/admin_backup.html:2396,5865` — admin dropdown options for UTM destinations (not visitor-facing).
    - `supabase/migrations/016_bot_utm_config.sql:48` — seed data for `bot_utm_configs` table; `backups/*/bot_utm_configs.json` files reflect this.
    - `scripts/seo-batch-update.py:126,127,132,212,353` — SEO automation script.
    - `CLAUDE.md:169` + `system_architecture.txt:211,1319` — docs claiming the file exists.
    If a 404 complaint comes in from another path, batch-fix all of these in one pass: `learning-master.html` → `training.html` for HTML/sitemap/schema, and update the migration SQL + admin.html dropdown + docs.
  - **Lesson learned.** When a cleanup commit deletes a file, run `grep -r "<filename>" .` over the repo before merging and either fix all references in the same commit or document the dangling pointers in primer.md as known-debt. The `dd61d29` commit didn't do this — Hillel hit one of those references 6 weeks later. Added to hindsight implicitly: cleanup commits need a "what links here" audit.
- **Prior task completed (2026-05-09):** v2 portal consolidation (`c435f9b`).
  - **Trigger.** Hillel pushed back on the parallel-v2 architecture decision from 2026-05-01 ("היה אמור רק לקחת את העיצוב לא לבנות חדש לגמרי"). With zero paying customers yet, he asked for a single professional portal: v2 with ALL features, no data loss. Per his guidance: drop community, do basic master + ambassador now, then flip the redirect.
  - **Discovery — v2 was more complete than primer claimed.** Survey showed v2 already had the lesson player (YouTube iframe), 4 tabs (notes/AI/resources/game), `loadNotesForCurrent`, `resetAiChat`, `renderResources`, `markLessonCompleted` (45s timer), `selectLesson`, hash routing, mobile drawer, A11y. The 2026-05-01 entry that called v2 a "welcome dashboard MVP only" was outdated by 2026-05-07's UX/UI upgrade pass. So actual gap was ONLY: master section + ambassador + community (and community got dropped).
  - **Ambassador port — `course-library-v2.html`.** New in-page view, no deep-link to old portal. HTML: `.lms-amb` section after `.lms-player` with link card (referral URL pointing to `free-portal.html?ref=USER_ID`, copy button + WA share + copy-message buttons), `.lms-amb-stat` (gold-accented "X חברים שהזמנת"), `.lms-amb-loginNotice` for unauthenticated, leaderboard card with top 10 (medals + names + counts). CSS adds `body.view-ambassador` toggle that hides welcome/player/master + 100 lines of glass styling matching v2 design tokens. JS: `showAmbassadorView` (clears video state, sets body class, calls loadAmbassadorView, pushes `#ambassador` hash), `loadAmbassadorView` reads user via `supabaseClient.auth.getUser()` then `window.Referrals.getMyCount`/`getLeaderboard`. `setupAmbassador` wires copy/WA/msg buttons + back nav. Sidebar item: previously `href="course-library.html#ambassador"`, now `href="#ambassador" data-nav="ambassador"`. Partners CTA on welcome dashboard: same change.
  - **Master port — `course-library-v2.html` (basic, forward-compatible).** New in-page view gated by `profiles.role === 'paid_customer' || 'admin'`. Constants `MASTER_WORKBOOKS` (6 PDFs: nlp-practitioner-main / master-course / master-nlp-techniques / full-techniques / submodalities / bonus-emotions, each with title + icon) and `MASTER_MODULES` (1 module currently: מפגש 1 רמות לוגיות וזהות, 8 chapters with YouTube IDs verbatim from old portal). HTML: `.lms-master` section with workbooks grid + lessons grid + a fullscreen `.lms-master-viewer` modal (iframe for both PDF viewer and YouTube player, ESC closes, download button). CSS adds `body.view-master` toggle + 80 lines of card/grid/viewer styling. JS: `checkPaidRole` queries `profiles.role`, sets `_isPaidCustomer`, unhides `#sbMasterItem` sidebar entry. `showMasterView` redirects non-paying users to `learning-master.html` (paywall) — important: anyone hitting `#master` in URL who isn't paid lands on the marketing page, not a broken section. `loadMaster` renders both grids. `openMasterWorkbook(i)` calls `supabaseClient.storage.from('workbooks').createSignedUrl(name, 600)` (10-min expiry) and shows the PDF in viewer. `downloadMasterWorkbook` does same with 300s expiry but opens in new tab. `playMasterLesson(videoId, flatIdx)` shows YouTube embed in viewer. Sidebar item is hidden by default; `boot()` calls `checkPaidRole` then `renderAll()` to refresh sidebar so master item appears for paid users.
  - **Community drop.** Sidebar entry "קהילת NLP" removed from `renderSidebarNav`. Resources renderer's "שאלות נפוצות" item that deep-linked to `course-library.html#community` replaced with "תוכנית השותפים" pointing to `#ambassador`. No community page in v2.
  - **View-switching state machine.** Refactored `showWelcome` to call `clearVideoState()` (kills 45s timer, sets iframe to `about:blank`) and clear all 3 view classes (`view-player`, `view-ambassador`, `view-master`). `handleHash` extended to recognize `#ambassador` and `#master` alongside `#m{m}l{l}`. `setupLessonNav` extended with delegated handler for `data-nav` attributes + `#ambassador`/`#master` anchors.
  - **Redirect flip — `free-portal.html` + `portal-questionnaire.html`.** 4 lines total. Free-portal: logged-in user with `portal_questionnaire_done` now lands on `course-library-v2.html` (was: `course-library.html`). Questionnaire: 3 places — pre-flight skip when already filled, success page CTA, auto-redirect on completion — all flipped to v2. `?ref=` preserved through redirect (no change to that logic).
  - **Out of scope (intentional).** Did NOT delete `course-library.html` — kept as fallback for existing bookmarks. Did NOT update other entry pages (index, landing-patient, landing-therapist) that may link to old portal — those serve organic traffic, not the campaign. Did NOT port community at all (Hillel said no). Did NOT add the focus-mode/keyboard shortcuts/confetti from old portal — v2 doesn't need them yet.
  - **Verified live.** Vercel deployed in ~30s. `curl https://www.therapist-home.com/pages/course-library-v2.html` returns content with `showAmbassadorView`, `MASTER_WORKBOOKS`, `view-ambassador` markers (14 hits across the 3 patterns). `curl https://www.therapist-home.com/pages/free-portal.html` shows `course-library-v2.html` redirect target.
  - **Pending Hillel real-user verification.** Need a logged-in browser session (which I cannot do from CLI) to confirm: (1) ambassador link card shows correct user_id, leaderboard renders, copy/WA buttons work; (2) master sidebar item DOES appear for admin role and DOES NOT appear for non-paid; (3) workbook signed URLs load PDFs in viewer; (4) lesson click → player → notes save → 45s completion still works (regression check). If anything breaks, single revert is `git revert c435f9b`.
- **Prior task completed (2026-05-09):** Early Meta Pixel + implicit consent on `free-portal.html` (`8aab76f`).
  - **Symptom.** 7-day variant block showed `link_clicks=102 → LPV=23 → leads=21` for var_a — impossibly high 91% LPV→Lead and only 22.5% click→LPV. Root cause: Meta Pixel was both **(1) gated behind `cookie_consent_v2 === 'all'`** (most ad visitors never click "accept all") AND **(2) loaded via `defer` at the bottom of body** so it fires only after DOMContentLoaded — too late for fast bouncers in iOS in-app browser. Net effect: ~78% of paid clicks invisible to Meta. Pixel/CAPI funnel can't measure variants accurately, AND the algorithm trains on a tiny biased sample.
  - **Fix #1 — `pages/free-portal.html`.** Inline Meta Pixel base snippet inserted in `<head>` immediately after `<title>` + `<meta name="description">` and before all other scripts (fonts, Turnstile, Supabase SDK, auth-redirect IIFE). Fires `init('1039436226809281')` + `track('PageView')` synchronously. Bails out early if `localStorage.cookie_consent_v2 === 'essential'` (explicit opt-out). Sets `window.__pixelInitedEarly = true` so the deferred `marketing-tools.js` knows to skip its own injection. `<noscript>` fallback img also added.
  - **Fix #2 — `js/marketing-tools.js` (5 surgical changes).**
    1. **`hasUserConsented()` flipped from `=== 'all'` to `!== 'essential'`** — implicit-consent model. Tracking ON unless user actively opts out. `null` (no choice yet) and `'all'` both pass.
    2. **`injectMetaPixel()` made idempotent** — added `if (window.__pixelInitedEarly || (window.fbq && window.fbq.loaded)) return;` so the inline head snippet is never duplicated.
    3. **Banner copy rewritten.** Was: "האתר משתמש בעוגיות. פרטים" + buttons "אישור / ללא מעקב". Now: "האתר משתמש בעוגיות לשיפור החוויה ולמדידת ביצועים. מדיניות הפרטיות" + buttons "הבנתי / ביטול מעקב". The banner is now informational, not a tracking gate.
    4. **Opt-out path now calls `fbq('consent', 'revoke')`** — tells Meta to stop browser-side data collection for this user from that point onward. The earlier PageView (already fired by inline head snippet) is unavoidable, but no further events.
    5. **DOMContentLoaded restructured.** Tracking initializes FIRST (line ~677), banner shown SECOND only if `!getConsentLevel()`. Banner no longer wraps `initializeTracking()`.
  - **Why this is the right call.** Israel doesn't strictly enforce GDPR-style pre-consent on first-party analytics; this is the standard for paid-traffic sites. Privacy policy + opt-out path remain — only the gating UX changed. Reversible in 5 minutes if a regulator complains.
  - **Deploy.** Single commit `8aab76f` → master → Vercel. Verified live ~15s after push: `__pixelInitedEarly` present in production HTML, `marketing-tools.js` shows `ביטול מעקב` + `__pixelInitedEarly` + `fbq('consent', ...)` markers.
  - **What to watch (next 7 days).** (1) Meta Events Manager: PageView volume should jump 4-5x for `free-portal.html` URL. (2) Daily 08:00 WhatsApp report: `link_clicks` ÷ `LPV` ratio should land in 70-90% (was 22.5%); `Meta=X / CRM=Y` gap should narrow significantly. (3) CPL should drop 15-30% over 7 days as the algorithm trains on a representative sample.
  - **Out of scope (intentional).** Did NOT add the inline pixel snippet to other entry pages (`portal-questionnaire.html`, `course-library.html`/`course-library-v2.html`, `landing-patient.html`, `landing-therapist.html`, `index.html`). The defer-loaded `marketing-tools.js` covers those — and with the new implicit-consent default, pixel will fire on DOMContentLoaded for all visitors who haven't opted out. If we later see those pages also under-reporting, we can add the head snippet there too. Did NOT touch portal-questionnaire's Lead/CompleteRegistration handlers (already correct).
  - **Two-portal inconsistency flagged but NOT fixed.** The Meta ad URL still points to `free-portal.html`, which (for logged-in users) redirects to the OLD `course-library.html`, not v2. So new visitors land on the new free-portal landing → if they sign up + come back logged in, they see the OLD portal. Either we wire the v2 redirect or accept the parallel state. Hillel asked about this — see his question in the chat.
  - **Pre-existing uncommitted state intentionally left untouched.** `.vercelignore`, `primer.md` (was being edited from prior session), `supabase/.temp/cli-latest`, plus a long list of untracked files (demos/, backups/, etc.). Only my two staged files were committed — primer update is happening in a separate commit now.
- **Prior task completed (2026-05-07):** UX/UI upgrade for `pages/course-library-v2.html` (`b430478`).
  - **Design tokens.** New `:root` with full token system (Color/Spacing/Radius/Elevation/Type/Motion). Palette shifted lighter: `--bg-primary: #0E2A2B` (was #04161b), `--accent-gold: #E9B949` (was #D4AF37). Legacy aliases preserved (`--petrol-deep`, `--gold`, etc.) for backward compat. Light-surface text tokens added: `--text-on-light-strong: #0F3D3E`, `--text-on-light-body: #1A2D33`, `--text-on-light-muted: #4A5959` (AAA contrast).
  - **Hero.** Removed duplicate play overlay (was rendering both a big "התחל ללמוד" button and a 64px play circle on the thumbnail — visual redundancy). Added "מודול X מתוך Y" + ≈duration in subtitle. Hero progress bar now reflects **module-level** progress (mp.done / mp.total) instead of global — more actionable.
  - **Sidebar.** `aria-current="true"` on the active lesson + new `.lms-sb-lesson--current` style (gold left-border + soft pulse). `<mark>` highlighting for matched search terms (gold bg). Debounce bumped 120 → 150ms. `selectLesson()` now calls `renderSidebarNav()` to refresh aria-current properly.
  - **Announcements.** Removed duplicate ambassador item from `announcements` array (was being shown twice — once in announcements, once as the dedicated `.lms-partners` card). New hierarchy: 1 `.lms-ann-featured` (gold-tinted, 2-line) + 3 `.lms-ann-small` items in `.lms-ann-secondary` column. Container query at 720px switches grid to 1.4fr/1fr.
  - **A11y.** Global `*:focus-visible{ outline:2px solid var(--accent-gold) }`. Expanded `prefers-reduced-motion` from atmos-only to all `*::before/::after`. `setupMobileDrawer()` returns focus to hamburger on close. `aria-hidden` toggles on `.lms-sb` per drawer state.
  - **Responsive.** Drawer breakpoint **768 → 900px** (small tablets get drawer too). Stats grid stack at **480 → 600px**. Sticky hamburger on mobile.
  - **Critical contrast bug discovered + fixed during deploy review.** Two screenshots from Hillel: first showed faint text from low-alpha rgba(255,255,255,.X) values that worked on the previous darker palette but failed on the new lighter one — replace_all'd ~25 occurrences to `var(--text-muted)` / `var(--text-primary)`. **Second screenshot** showed white text on white in stats cards: the `replace_all` had **also matched the hex values inside the `:root` definitions themselves**, creating self-references like `--text-on-light-strong: var(--text-on-light-strong)` → CSS unset → inheritance fell through to body's white. Restored hex values in `:root`, also bumped card backgrounds from `.96 alpha` to fully solid `#FFFFFF` so the new lighter page bg can't bleed through.
  - **Memory updates** (during user review windows, parallelized):
    - `feedback_text_contrast_must_be_readable.md` — added rule 8: after every replace_all of hex → token, run `Grep --(\w[\w-]*?)\s*:\s*var\(--\1\)` to detect self-references. This bug has now occurred 3 times on this file — explicitly noted "not a fourth time".
    - New `feedback_parallelize_review_time.md` — when user is reviewing (browser/screenshot/output), use that window for parallel work (memory updates, docs, next-step prep). Don't sequentialize. His time > my time.
  - **Deploy.** `b430478` → master → Vercel. Verified live: `curl https://www.therapist-home.com/pages/course-library-v2.html?v=...` returns HTTP 200 with new tokens (`#0E2A2B`, `--accent-gold:`) present.
  - **Out of scope.** Stage 3 (master section for paid customers + 6 PDF workbooks on the v2 page). Replacing old `course-library.html` as default — both serve in parallel for now until full validation. Extracting modules data + supabase logic into shared JS file (TEMP duplication acceptable).
- **Prior task completed (2026-05-04):** NLP Retention Phase 2 + Phase 3 — admin approval tab + dashboard trigger button + 2 micro-iterations.
  - **Phase 2 — admin approval tab.** New `js/admin/admin-retention.js` (~340 lines) loads from `retention_messages`, renders date-grouped table with 7 status tabs (draft/approved/sent/rejected/opted_out/failed/all), bulk-approve, CSV export. Modal review surfaces the student's `vision_one_year` quote prominently in gold (the load-bearing copy element — Sonnet mirrors it in the message), `why_nlp` context, editable textarea with word counter. New sidebar entry "שימור — נוטשי קורס" with badge. New view `retention-view` with collapsible info-box ("איך זה עובד?"), 4 status stat cards, search, bulk bar.
  - **New migration `20260503130000_retention_admin_rls.sql`.** Admin SELECT + UPDATE on `retention_messages` (anon JWT, role='admin' check). INSERT remains service-role only (Python sync OR Edge Function). DELETE never granted — rejection is soft state for return-rate analytics.
  - **Hash deeplink router added to `admin-state.js`.** `navigateFromHash()` handles `admin.html#retention` (and any other registered VIEW_GROUPS entry). Improvement applies to all admin tabs, not just retention.
  - **Phase 3 — Edge Function trigger from dashboard.** `supabase/functions/retention-run/index.ts` (~530 lines, self-contained) ports the entire `nlp-retention/` Python pipeline to Deno/TS: lessons_map embedded as TS const (cached in system block), classify_why deterministic dictionary, normalizePhone matching crm-bot pattern, computePreferredHour with Asia/Jerusalem +3h approximation, findInactiveStudents mirroring supabase_reader.py logic, processStudent with race-safe dedup re-check (14-day window by user_id+next_lesson_video_id), parallel batches of 3 students. Anthropic SDK via `https://esm.sh/@anthropic-ai/sdk@0.32.1`, prompt caching with `cache_control: ephemeral` on system prompt + lessons_map blocks (~90% token savings after first student). Hard cap 30 students per call (150-second Edge Function timeout safety) — if more remain, response includes `remaining > 0` and admin clicks again. Auth check on admin role before any work. Returns `{scanned, processed, inserted, skipped, failed, remaining, errors[]}`.
  - **Trigger button in admin.** Gold gradient action box at top of `retention-view`: "סרוק נוטשים → ייצר טיוטות" with spinner during call, status text below updates with results live. JS handler `runRetentionScan()` posts to `/functions/v1/retention-run` with bearer token, parses summary, calls `loadRetention()` to refresh table.
  - **Standalone "save edits" button (per Hillel's request).** New `saveRetentionEdit()` updates only `message_text`, preserves `status='draft'`. Lets admin iterate on copy across multiple drafts in passes — edit several, come back later to approve. Hidden for non-draft rows. Turquoise styling distinguishes from gold "approve" and red "reject".
  - **Em-dash ban in generation (per Hillel's request).** SYSTEM_PROMPT rewritten to remove every `—` it was using in examples (Sonnet copies dash style from prompt body more than from explicit rules), plus an explicit rule forbidding all dash characters as separators (`—`, `–`, `-`, `−`) — only allowed inside compound words (תת-מודע, אי-וודאות). Includes self-check step before returning. Function redeployed.
  - **Migration immutability discovered.** Started by editing the not-yet-pushed `20260502120000_retention_messages.sql` to add admin RLS — but `db push` showed "Remote database is up to date" because that migration was actually applied yesterday (with the attribution fixes batch). Reverted local edit + created new migration `20260503130000_retention_admin_rls.sql` with just the additions. Lesson: never edit applied migrations, always add new ones.
  - **Deploy.** Three commits: `6998d1f` (Phase 2 — admin tab + admin RLS migration + hash routing) → `e4ba33e` (Phase 3 — Edge Function + trigger button) → `ea47e48` (save button + no-dash rule). All pushed to master, Vercel auto-deployed all three. Edge Function deployed twice (initial + after prompt update). Static verification via chrome-devtools MCP confirmed all markers in production after each deploy.
  - **Out of scope.** Phase 4 (sender cron via Green API), Phase 5 (opt-out webhook handler), Phase 6 (returns tracker that correlates `course_progress.completed_at` with `sent_at` within 14-day window), daily 08:00 cron auto-trigger. All planned, none implemented yet.
- **Prior task completed (2026-05-03):** NLP game deep-fix after customer reports of "game stops working at a certain stage". Migration applied to production. JS code change pending git push.
- **Last task completed (2026-05-03):** NLP game deep-fix after customer reports of "game stops working at a certain stage". Migration applied to production. JS code change pending git push.
  - **Symptom.** Customers reported the game breaks "at a certain stage". Static audit cleared the data files (M1-M7 all pass `node --check`); the bug is in the engine logic.
  - **Root cause #0 (deepest — discovered during deploy).** The `nlp_game_players` table **was never created by any migration** in version control. JS engine called `window.supabaseClient.from('nlp_game_players')...` since the game shipped, but every save/load wrapped in try/catch + console.warn was failing silently and falling back to localStorage (line 543). Result: cross-device sync **never** worked for any logged-in user. localStorage on phone = progress on phone only; opening desktop = empty = "the game restarted at a stage I already passed". This is the most plausible customer-facing symptom.
  - **Root cause #1 (P0).** Even when Supabase WAS available, `loadPlayerData()` (js/nlp-game.js:502-528) builds a fresh `playerData` object from the row but explicitly omits `migrationVersion`. `migrateProgress()` (line 472) then sees `playerData.migrationVersion = undefined`, so `if (>= 2) return` falls through and the function **wipes `completedLessons = {}`** on every fresh load. Same destructive symptom from a second angle.
  - **Root cause #2 (P1 UX).** `showCompletionScreen()` (line 2687) sent users back to the just-completed module via `openModule(currentModule.id)`. End of a 100%-complete module = user sees "✅ ✅ ✅" with a tiny back arrow. Looks broken.
  - **Root cause #3 (P1 sync).** `perfectLessonsList`, `weeklyActivity`, `moduleAccuracy`, `longestStreak` were defined in `getDefaultPlayerData()` but never read from Supabase or written back. Cross-device users silently lost achievements, weekly activity, perfect-lesson streaks.
  - **Root cause #4 (P2 inconsistency).** Home-screen module lock used `< 50%` previous-module progress; in-module lesson lock used `< 100%` previous-lesson. Mismatched criteria — module appeared unlocked at 50% but its lessons weren't all reachable.
  - **Fix #1 (P0).** `migrateProgress()` now bails out early if `completedLessons` has any entry (defense in depth — never wipe existing progress even if version field is missing). `loadPlayerData()` maps `migration_version`. `saveToSupabase()` + `createSupabaseRow()` write `migration_version` on every save.
  - **Fix #2 (P1 UX).** New `showCompletionScreen()` calls helper `findNextLessonInCurrentModule()` → `findNextModule()` and renders dynamic primary CTA: "השיעור הבא: {title} ←" / "המודול הבא: {title} ←" / "סיימת את המסע! 🏆". Secondary "חזרה לתפריט" preserved.
  - **Fix #3 (P1 sync).** All 4 omitted fields now mapped in load + saved in write paths. Default values match `getDefaultPlayerData()`.
  - **Fix #4 (P2 lock unification).** New helper `isModuleLocked(index)` — single source of truth. Rule: previous module 100% complete OR user already started this module (grandfathering). All 3 home-screen sites + daily-challenge `availableModules` migrated.
  - **Fix #5 (P2 defensive).** New `showError(msg)` helper using `#toast`. Null-checks added to `startLesson()` (1611) and `renderExercise()` (1720) — no more silent no-ops on missing lesson/exercises (per hindsight 2026-04-28 silent-update rule). New `initBeforeUnload()` flushes the 1s Supabase debounce on tab close so quick-close-after-correct-answer doesn't lose XP.
  - **Migration `20260503140000_nlp_game_player_full_state.sql`.** **Creates** `nlp_game_players` (CREATE TABLE IF NOT EXISTS) with the full 21-column schema the JS engine expects, including the 5 new columns (`migration_version`, `perfect_lessons_list`, `weekly_activity`, `module_accuracy`, `longest_streak`). Belt-and-suspenders ADD COLUMN IF NOT EXISTS for every column too, in case the table was manually created out of band. Backfills `migration_version=2` on existing rows. RLS: own-row read/write per user, full access for admin (mirrors `nlp_game_leaderboard` pattern from migration 042). Applied successfully via `npx supabase db push --include-all` on 2026-05-03.
  - **No content changes.** M1-M7 data files untouched. nlp-game.html, css, leaderboard, knowledge-base unchanged. Engine grew 3,297 → 3,424 lines.
  - **Verification done.** All 11 game JS files pass `node --check`. Manual code review confirms all helpers wired (`isModuleLocked`, `showError`, `initBeforeUnload`, `findNextLessonInCurrentModule`, `findNextModule`). Visual end-to-end pending Hillel.
  - **Out of scope.** M1's 1-lesson/48-exercises and M7's 2-lessons/49-exercises content anomaly. Story Builder, Daily Challenge, AI Mentor (Ram), leaderboard ranking — all left as-is.
  - **Deploy.** Step 1 done: migration applied (CREATE TABLE was the actual op — table didn't exist before; ADD COLUMN IF NOT EXISTS were no-ops). Step 2 pending: git push for engine fixes (Vercel auto-deploys).
- **Prior task completed (2026-05-03):** lead_attribution backfill from browser-side data after questionnaire + signup.
  - **Symptom.** Hillel's morning daily report said "4 נרשמו" but his CRM showed 6 leads in the last 24h. Analysis revealed **4 of yesterday's leads (Tomer/Eden/Daniel/Shlomi) had ZERO technical fingerprint** in `lead_attribution` — no fbclid, no landing_url, no IP, no device, no browser. Only `self_reported_source` from the questionnaire form. For Tomer & Shlomi, Meta's Lead pixel actually fired in the same hour they registered (via hourly Meta API breakdown verification), proving they came from the campaign — but UTM was lost in the funnel (likely iOS in-app browser strip).
  - **Root cause #1.** `portal-questionnaire.html` does direct INSERT to `portal_questionnaires` (NOT through submit-lead — `portal_questionnaires` isn't in `submit-lead/index.ts:18` ALLOWED_TABLES). The DB trigger `tg_portal_q_to_attribution` creates a `lead_attribution` row but only with utm_*/how_found from columns — fbclid/landing_url/device live in localStorage and never reach the server.
  - **Root cause #2.** `ensureProfile()` in supabase-client.js had a manual `lead_attribution` INSERT but only inside `if (!existing)`. The `handle_new_user()` DB trigger from 2026-03-15 auto-creates the profile row server-side, so `existing` is always truthy → the INSERT branch never fired → every signup in the last ~2 months has missing/empty profile attribution.
  - **Root cause #3 (silent for ~2 weeks).** `saveCapiBrowserData()` was using anonKey for PATCH on `lead_attribution`, but RLS denies anon UPDATE. Every call has been silently 401-ing since 2026-04-22 — `meta_fbc/fbp` are NULL across ALL 9 leads in the 24h sample, not just the 4 untracked.
  - **Fix #1 (daily report transparency).** `meta-ads/skills/meta-ads/scripts/daily_health.py` — added `fetch_crm_full_breakdown()` + `format_crm_breakdown_block()`. New WhatsApp section: "📥 ב-CRM אתמול: X לידים — Y מהקמפיין הוריאנטי, Z מקמפיין מטא אחר, W חשד שאיבד UTM". Runs tomorrow 08:00 via existing cron, no deploy needed.
  - **Fix #2 (questionnaire + signup attribution).** `js/marketing-tools.js` — new `saveFullAttribution(linkedTable, linkedId)` UPSERT helper (PATCH first with `Prefer: return=representation` to detect zero rows; INSERT if no match). Added `_getBearerToken()` that grabs the user session token instead of anonKey. `saveCapiBrowserData` also fixed to use bearer token. `pages/portal-questionnaire.html` captures the new questionnaire id (`.select('id').single()`) and calls `saveFullAttribution('portal_questionnaires', insertedQ.id)` after INSERT. `js/supabase-client.js` — moved the `lead_attribution` write OUT of the `if (!existing)` branch in `ensureProfile()`; replaced manual INSERT block with `saveFullAttribution('profiles', user.id)` so it runs every login (UPSERTs missing rows for users created via the auth trigger).
  - **Fix #3 (CAPI).** No code change. Investigated `meta-capi/index.ts` + `enqueue_qualified_lead()` trigger — already operational, 24/24 events sent successfully (Tomer's QualifiedLead fired at 23:57 IL, Eden's at 21:22). Recommendation to Hillel: change Meta Ads adset optimization from `Lead` to `QualifiedLead` (UI-only change in Ads Manager).
  - **Migration `20260503090000_lead_attribution_auth_update.sql`.** Two narrow RLS UPDATE policies — `auth_update_own_profile_attr` (linked_table='profiles' AND linked_id=auth.uid()) and `auth_update_own_q_attr` (linked_table='portal_questionnaires' AND EXISTS in own pq). No service-role policy changes; admin RPCs still use SECURITY DEFINER.
  - **Deploy.** `npx supabase db push --include-all` (with retention migration temporarily stashed → restored after) → commit `5cdb48a` for the questionnaire fix → commit `f3ff7f3` for the signup fix → both pushed to master. Vercel auto-deployed both.
  - **Side deploy (Hillel's request).** `20260502120000_retention_messages.sql` — pushed at the same time per his "תדחוף" approval. Tables `retention_messages` + `retention_optouts` now exist in production with closed RLS. Commit `8ba7aea`.
  - **Confirmed working.** Migration applied without errors. JS syntax-checked. End-to-end verification requires next live lead to come through — anyone who fills the questionnaire from now on should have full attribution row (fbclid, landing_url, device, browser, fbc/fbp).
  - **Out of scope (not fixed today):** (1) Backfill historical leads — data lived only in localStorage which is gone. (2) `portal_questionnaires` still bypasses submit-lead Edge Function entirely (Turnstile, IP/geo, sanitization aren't applied). Future refactor opportunity. (3) Hillel's recommended Meta Ads Manager change (Lead → QualifiedLead optimization) — manual UI step, not done by Claude.
- **Prior task completed (2026-05-02):** UTM persistence fix — primary tables were silently dropping `utm_*` fields.
  - **Symptom.** Meta's `daily_health.py` daily report flagged "Meta=4 leads, CRM=0 attributed" for over a week. Investigation showed `contact_requests`/`profiles` rows had ALL utm_* NULL; `portal_questionnaires` had `utm_source`+`utm_campaign` but `utm_content`+`utm_term` NULL.
  - **Root cause #1 (Edge Function).** `submit-lead/index.ts` accepted `attribution` payload from the client but only wrote it to `lead_attribution`. The primary INSERT (line 297-299) used `cleanData` which never received the UTM. Mirror INSERT (line 320) had the same gap.
  - **Root cause #2 (portal-questionnaire.html).** Direct `supabaseClient.from('portal_questionnaires').insert()` at line 884 only wrote 3 of 5 UTM fields (utm_content + utm_term were missing).
  - **Fix.** Added new helper `backfillUtmFromAttribution(row, attribution)` to submit-lead — copies all 5 UTM keys from `attribution.last` to the row before INSERT, only when row doesn't already carry them (client-provided wins). Called on both `cleanData` and `cleanMirror`. Added `utm_content` + `utm_term` to portal-questionnaire's direct INSERT.
  - **Deploy.** `npx supabase functions deploy submit-lead --no-verify-jwt` succeeded. Commit `a98c6a1` pushed to master — Vercel auto-deploys portal-questionnaire change.
  - **Confirmed working.** Real-time CAPI events 1:1 with portal_questionnaires for last 2 days (6/6 on 02/05, 5/5 on 01/05). CAPI architecture (`meta-capi` real-time + `capi-queue-processor` deferred) was already correct — no fixes needed there.
  - **Side-effect cleanup (Meta side):** Set `creative.url_tags` on all 4 active ads (var_a/b/c/d) to `utm_source=facebook&utm_medium=paid_social&utm_campaign={{campaign.name}}&utm_content={{ad.name}}&utm_term={{adset.name}}`. This was the missing piece on Meta's side — without it, ~40% of clicks (private mode, ITP) reached the landing page WITHOUT UTM. Done from the meta-ads skill, not committed to this repo. See meta-ads/primer.md.
  - **Out of scope (not fixed today):** Google OAuth signup loses UTM through redirect; 13 historical leads from 01/05 stay anonymous (no backfill possible).
- **Prior task pending:** Liquid Glass V1 portal — Stage 1 (welcome dashboard) (2026-05-01, NOT YET PUSHED):
  - **Source.** Claude Design handoff bundle (`Beit HaMetaplim Design System-handoff (2).zip`) extracted to `C:\Users\saraa\AppData\Local\Temp\dsys\`. Contains 3 desktop variations (V1 balanced, V2 cinematic, V3 timeline) + 3 mobile variants. README directs implementer to `ui_kits/lms-portal/lms.html` and its imports.
  - **Demo bundle staged.** `demos/lms-glass/ui_kits/lms-portal/lms.html` + `v1-balanced.js` + `mobile-v1-v2.js` + `colors_and_type.css` + `assets/logo-square.png` copied for visual review with V2/V3 buttons stripped. `demos/` is in `.vercelignore`, never deployed.
  - **Approved scope.** V1 only. MVP = welcome dashboard only (header + sidebar + hero + 3 stats + announcements + partners + FAB). Lesson click → deep-link to existing portal via `course-library.html#m{m}l{l}` (existing hash routing at lines 6141-6156 already supports this — no edit needed to old portal).
  - **New file: `pages/course-library-v2.html`** (1,228 lines). Self-contained: inline `<style>` (~530 lines, all selectors scoped under `.lms-glass`) + inline `<script>` (~330 lines). Auth gate copied from `course-library.html:5406-5421` with `?dev=1` bypass for QA only. Loads `js/supabase-client.js` + `js/marketing-tools.js`. Reuses `window.CourseProgress.getCompletedVideoIds('nlp-practitioner')` for progress merge, `window.Profiles.get(user.id)` for paid role check (shows "מאסטר" sidebar item if `paid_customer`/`admin`). Duplicates `modules` array (51 lessons / 7 modules) and `wdBulletinData` inline — TEMPORARY until Stage 6 extraction to `js/lessons-data.js`.
  - **Render functions** (all read-only, mirror `course-library.html` helpers): `getNextLesson()` (6708-6722), `parseDuration()` (6843), `getRemainingTime()` (6852), `getCurrentModuleProgress()` (6868), `getStudyStreak()` (6819-6841). Updates: `hdProgress*` + `sbProg*` + `heroProg*` (3 progress bars wired to single `renderProgress()`).
  - **Mobile.** Sidebar becomes drawer (slide-in from RTL right) at ≤768px, hamburger button in header. Hero stacks 1-col at ≤420px. Stats grid → single column at ≤480px. Header CTA wrap hidden ≤980px (next-lesson info still in hero). Search filter auto-expands modules with matching lessons.
  - **Visual review.** Local Python http.server `scripts/serve.py 8765` running in background. URL: `http://localhost:8765/pages/course-library-v2.html?dev=1` opens with bypass. Hillel viewing now.
  - **Stages 2-5 not yet started.** Stage 2 = video player + tabs in glass. Stage 3 = master section (20 lessons + 6 PDF workbooks) in glass. Stage 4 = redirect `free-portal.html` to V2. Stage 5 = delete `course-library.html` + rename V2.
  - **Open follow-ups.** (1) Hillel's visual approval pending. (2) Decide whether to keep `?dev=1` bypass after launch (low risk, QA-useful) or strip it. (3) When Stage 4 is approached: also need to update Vercel `vercel.json` rewrites if URL conventions change. (4) `pages/course-library.html` still has `<link rel="apple-touch-icon" href="../assets/logo-square.png">` only — V2 inherits the same. PWA icons mentioned in 2026-04-30 primer don't exist on disk; verify if shipped or only documented.
  - **Local server cleanup.** `bgxm0fj7n` background task running `python scripts/serve.py 8765`. Kill before closing session: `taskkill //F //PID <pid>` or just close the terminal window.
- **Prior task completed:** Mobile UX rewrite + installable PWA (2026-04-30, commit `4b75749`):
  - **Scope.** Learning portal pages only (course-library.html, profile.html, login.html). Admin dashboard, signup steps, marketing pages explicitly excluded.
  - **PWA infrastructure.** `manifest.json` upgraded to standalone with `start_url=/pages/course-library.html` (opens directly to portal from home screen, not the marketing page), `id`, `orientation=portrait`, `categories=[education,lifestyle]`, 4 dedicated icons + 2 shortcuts (portal + profile). Old `assets/logo-square.png` was 289x266 — too small for proper icons. Generated 4 new PNGs via Pillow (`scripts/generate_pwa_icons.py`): `pwa-icon-192.png`, `pwa-icon-512.png`, `pwa-icon-maskable-512.png` (logo at 65% safe zone for Android adaptive), `apple-touch-icon-180.png`. All on `#003B46` deep-petrol opaque background.
  - **Service Worker rewrite (`sw.js`).** Hybrid strategy: cache-first + stale-while-revalidate for static (`.css/.js/.png/.jpg/.svg/.woff/.ico`), network-first with cache fallback for HTML, pass-through for `supabase.co`/`googleapis.com`/`youtube.com`/`challenges.cloudflare.com` (auth + video + analytics never cached). Versioned cache (`portal-v3-2026-04-30`) auto-evicts old generations. `skipWaiting` + `clients.claim` so new SW activates without tab refresh. Pre-cache list expanded from NLP-game-only (~14 files) to portal-wide (~30 files including theme.css, components.css, supabase-client.js, all 3 portal HTML pages, mentor avatar, hero images).
  - **PWA install prompt (`js/pwa-install.js`).** Single bootstrap file: registers `/sw.js` on load, then waits 30s before showing install UI. Android: catches `beforeinstallprompt`, shows custom banner ("להוסיף למסך הבית? פותחים את הפורטל בקליק אחד, כמו אפליקציה") with "התקן" / "לא תודה" buttons; calls `prompt()` on accept, persists `pwa_installed=1`. iOS Safari (no native prompt): shows trigger banner with "איך?" → opens 3-step modal explaining "שתף → הוסף למסך הבית → הוסף". State stored in localStorage: `pwa_install_dismissed_at` (14-day cooldown), `pwa_installed`. Detects standalone mode (`display-mode: standalone` or `navigator.standalone`) and skips entirely.
  - **course-library.html mobile rewrite.** Added `viewport-fit=cover` + `apple-mobile-web-app-title` meta tags. Updated favicon links to new icons. Inserted ~210-line CSS override block at end of inline `<style>` (last-wins cascade beats existing 4000-line CSS): `100dvh` for `.lms-container` with `100vh` fallback via `@supports`; `env(safe-area-inset-*)` on `.course-header` (calc 64+top), `.feedback-fab` (calc 16px+bottom), `.sidebar` (top calc with safe-area-top); 380px breakpoint hides progress pill, stacks welcome hero, makes quick-stats horizontal-scroll with snap; modals → bottom-sheet on mobile (`align-items: flex-end`, `border-radius: 18px 18px 0 0`, `max-height: 92dvh`, sheet-up animation); body scroll lock via `body.modal-open { overflow: hidden; position: fixed; }`; AI chat fullscreen via `.ai-fullscreen` class injected by mobile helpers; sticky video nav bar at bottom on mobile with `padding-bottom: env(safe-area-inset-bottom)`; touch targets enforced 44px on .btn/buttons/role=button, 48px on .modal-close; `font-size: 16px` on inputs to prevent iOS focus-zoom.
  - **portal-mobile-helpers.js (`/js/portal-mobile-helpers.js`).** Mobile-only JS augmentation: MutationObserver on all `.modal-overlay` elements to toggle `body.modal-open` when `.active` class appears; MutationObserver on `#aiChat` to toggle `.ai-fullscreen` when chat becomes visible AND viewport ≤1024px (with auto-injected close button switching back to lesson tab); sidebar: ESC key closes, swipe-right (≥60px, more horizontal than vertical) closes, MutationObserver syncs `aria-expanded` on `#mobileSidebarToggle`.
  - **profile.html mobile rewrite.** Added `100dvh` fallback. Header padding now uses safe-area-top/inline. New 380px breakpoint: stats grid → 1fr (single column, horizontal layout per card), icon size 48→32, progress rings 180→140. Anti-zoom (16px) + 44px button minimum below 768px.
  - **login.html mobile rewrite.** Anti-zoom already correct. `100dvh` fallback added. Buttons (`.btn-login`, `.btn-google`) now `min-height: 48px`. Container padding uses safe-area-inline. New 480px + 380px breakpoints with progressive shrink. Turnstile widget centered + max-width 100%.
  - **Shared CSS tokens.** `theme.css`: `--touch-target-min: 44px`, `--safe-area-{top,bottom,left,right}` variables. `components.css`: mobile-only globals @media (max-width: 768px) — input/select/textarea font-size 16px, button/.btn/[role=button] min-height 44px, .modal-close 44x44; `.safe-area-{top,bottom,inline}` opt-in helper classes.
  - **Service Worker registration on portal pages.** course-library.html, profile.html, login.html now load `/js/pwa-install.js` before `</body>` (also registers `/sw.js` with scope `/`). course-library.html also loads `/js/portal-mobile-helpers.js`.
  - **Verification.** Local Python http.server (`scripts/serve.py` — explicit chdir to handle Hebrew paths that break inline `python -m http.server`). Chrome DevTools mobile emulation @ iPhone SE 375x667. course-library.html itself blocked by `auth gate` at line 5615 (redirects to free-portal.html when no Supabase session) — couldn't render the portal layout in DevTools without faking auth. login.html screenshot perfect (large gold buttons, properly spaced inputs, RTL correct, safe-area-bottom respected for bottom toast). Hillel reviewed login screenshot, said "בדקתי נראה מעולה תעלה לגיט" → push to master, Vercel auto-deploy.
  - **Open follow-ups.** (1) Validate PWA install on real iPhone Safari + real Android Chrome after Vercel deploy completes. (2) Course-library auth gate prevents in-DevTools rendering — consider adding `?dev_bypass=1` query-param escape hatch for QA. (3) Lighthouse Mobile audit not run (page didn't fully load in DevTools due to auth gate). (4) Maskable icon safe zone is 65% — verify on Android adaptive icon mask shapes (squircle/circle/rounded-square). (5) PWA install banner cooldown is 14 days — adjust if Hillel finds it annoying or too rare.
- **Prior task completed:** Phase 3 — unified "מקור הלקוחות" dashboard (2026-04-30):
  - **Problem.** Two parallel dashboards ("סטטיסטיקות האתר" GA4 + "טראפיק"/"אינסטגרם" lead_attribution) showed conflicting numbers per source. Hillel could not trust either for business decisions. Two separate widgets, two reconciliation models, no single source of truth.
  - **Solution.** New "מקור הלקוחות" tab as default sub-tab under "מקורות תנועה". One funnel grid per source: visitors (from GA4) → leads → registrations → paid customers + 3 conversion %s, with healthy-range color coding. Old GA4 + lead_attribution + Instagram tabs preserved as `(ישן)` sub-tabs for 2 weeks before removal.
  - **Migration `20260430120000_admin_unified_source_funnel.sql`.** Two SECURITY DEFINER RPCs: `admin_unified_source_funnel(days)` returns JSONB { totals_per_stage, per_source[], mismatches[] }. `admin_source_reconciliation_weekly()` returns this-week vs last-30d UTM/how_found coverage. Both apply admin guard + DISTINCT-person logic per hindsight 2026-04-19 ("rows ≠ people").
  - **Source resolution rule.** Canonical source per lead: COALESCE(LOWER(last_utm_source), bucketed how_found, 'unknown'). how_found bucketing handles Hebrew + English variants via ILIKE patterns (אינסטגרם/instagram/IG → instagram). Mismatches (UTM and how_found map to different canonicals) surfaced in a banner + drill-down modal.
  - **Transparency notes (Hillel's explicit ask "כל פער ייכתב בתוך הדשבורד עצמו").** 5 banner types: info (always), historical-floor warning, GA4 error fallback, mismatch alert, KPI strip. Tooltips on every column with healthy ratios + red flags. "Unknown" row has annotated icon explaining 3 reasons leads fall there. Weekly reconciliation modal as a one-click sanity check.
  - **Files.** `supabase/migrations/20260430120000_admin_unified_source_funnel.sql` (new RPC + reconciliation RPC). `js/admin/admin-sources.js` (new, ~330 lines — RPC fetch, GA4 merge, render, mismatches modal, reconciliation modal). `js/admin/admin-sources.css` (new, theme-variable only — light/dark safe). `js/admin/admin-utils.js` (VIEW_GROUPS.traffic now has 4 sub-views with `'sources'` as default; lazy-load hooks for sources/analytics/instagram). `pages/admin.html` (CSS link, script tag with `?v=8` cache-buster, sub-tab structure replacing the simple traffic-header).
  - **Verification needed in browser** — admin loads → "מקורות תנועה" → table populates with non-zero leads/registrations/paid; switching sub-tabs to ישן variants still works; mismatch banner only appears if mismatches exist; weekly reconciliation modal opens.
- **Prior task completed:** Phase 2 — fix "ישיר / לא ידוע" attribution bug for recent portal_q rows (2026-04-30):
  - **Root cause confirmed.** The 2026-04-29 unification migration was a one-shot UPDATE+INSERT backfill — no AFTER INSERT trigger was added, so portal_questionnaires/contact_requests created after 29.4 had no `lead_attribution` row. Additionally, RPC `admin_get_all_leads()` v3 returned only `utm_source` (dropping `utm_medium`/`utm_campaign`), so when attribution was missing, chip rendering lost the medium suffix and degraded to "ישיר / לא ידוע".
  - **2 new migrations.** `20260430090000_lead_attribution_triggers.sql` — adds `tg_portal_q_to_attribution()` + `tg_contact_request_to_attribution()` AFTER INSERT triggers (COALESCE update if attribution row exists, else INSERT; SECURITY DEFINER + search_path=''). Also runs idempotent gap-window backfill (NOT EXISTS guards) for 29.4 → now. `20260430090100_admin_get_all_leads_v4.sql` — drops v3, recreates with `utm_medium TEXT, utm_campaign TEXT` appended to RETURNS TABLE; Source 1 uses COALESCE(pq.utm_*, p.utm_*), Source 2 uses cr.utm_*. Final NOTIFY pgrst schema reload. Note: landing_url/referrer NOT added to RPC — those columns don't exist on portal_q/contact_requests; they live on lead_attribution and are populated by submit-lead Edge Function only.
  - **Verification (via `scripts/verify_phase2_attribution.py`).** Test 1+2 PASS: synthetic portal_q INSERT (with admin user_id to satisfy enqueue_qualified_lead trigger's RECORD bug) auto-created attribution row with all 4 fields populated correctly. Test 3 PASS: 2/2 post-29.4 portal_q rows with source data now have attribution. Test 5: 301 portal_q attribution rows total (was 240 before — backfill added 61). Test 4 (RPC v4 columns) requires admin auth — can only verify in browser by loading admin.html and checking chips render with utm_medium suffix.
  - **No frontend changes.** `formatLeadSource()` in `js/admin/admin-utils.js:298-317` already implements correct fallback chain — once trigger fills attribution rows + RPC exposes utm_medium, chips automatically render correctly.
  - **Pre-existing bug discovered (NOT fixed in this session):** `enqueue_qualified_lead()` trigger function at `20260426113000` accesses `v_profile.email/phone/full_name` even when `NEW.user_id IS NULL` (RECORD never assigned). Throws PG error 55000 "record v_profile is not assigned yet". Production paths always set user_id so this never fires for real submissions, but blocks any test INSERT without a user_id. Worth fixing in a follow-up.
- **Prior task completed:** Phase 1 — Meta Ads daily spend integration (2026-04-29):
  - **2 new tables.** `meta_campaign_spend_daily` (date+campaign_id unique, spend/impressions/clicks/leads/CPM/CTR/reach) + `meta_campaign_to_utm` (manual mapping override; auto-populated by Edge Function on first sight). Both RLS service-role-only.
  - **3 admin RPCs.** `admin_get_meta_spend(start, end)` for read; `admin_get_meta_campaign_map()` + `admin_upsert_meta_campaign_map()` for mapping table maintenance. SECURITY DEFINER with role='admin' check.
  - **Edge Function `fetch-meta-ads-spend`.** Calls Meta Marketing API v25 `/act_<id>/insights` per-day for `?days=N` (default 1, capped 90). Counts only `lead` action_type to avoid Meta's 6-way duplication of the same conversion. Auto-fills mapping table with new campaign_ids found in API response. Returns summary JSON: `days_processed`, `rows_upserted`, `errors[]`. HTTP 207 if any errors.
  - **Cron schedule.** `meta-ads-daily-spend` at `0 3 * * *` UTC (06:00 Israel during DST, 05:00 standard) calling Edge Function with `?days=1` for "yesterday".
  - **Schema reload migration** (`20260429120200`) — issued `NOTIFY pgrst, 'reload schema'` after deploying RPCs/tables to bypass PGRST202/205 schema-cache lag.
  - **Verification.** Backfilled 14 days, 7 rows upserted (only days with spend appear). Numbers cross-checked vs `fetch_insights.py` from the meta-ads Python skill — match exactly (e.g. 2026-04-22: ₪33.71 / 699 impressions / 40 clicks / 3 leads / CTR 5.72% / CPM ₪48.23). Total backfill: ₪413.88 / 21 leads / 7 active days from 15-23.4.2026.
  - **Hillel's UTM action item.** Current Meta ad URL `free-portal.html` carries no UTM. Until UTM is added at Ads Manager (or override row inserted into `meta_campaign_to_utm`), spend rows can't join to `lead_attribution` for ROI. The mapping table now has 1 auto-row: `campaign_id=120242071812240239` → `utm_campaign=<slugified Hebrew>`. Hillel will add `utm_source=facebook&utm_medium=cpc&utm_campaign=cold-leads-meta` (or chosen value) at the AdSet level in Ads Manager + update the mapping row.
  - **Secrets added to Supabase project `eimcudmlfjlyxjyrdcgc`:** META_ACCESS_TOKEN, META_AD_ACCOUNT_ID, META_API_VERSION, META_APP_ID, META_APP_SECRET. Pulled from `~/.secrets/meta-ads.env` (regenerated by Hillel 2026-04-29 after old token returned `API access blocked` code=200).
- **Prior task completed:** Traffic source unification — Phases 1→3 (2026-04-29):
  - **Phase 1 — Diagnostic.** SQL audit revealed: only 291/299 portal_questionnaires have meaningful UTM/how_found data. All other lead tables (patients/therapists/contact_requests/sales_leads/profiles = 386 rows total) have empty utm_source columns. Of 613 attribution rows, 138 had ANY source signal in `last_utm_source`; 0 in `first_utm_source`. Earlier backfills wrote only to `last_*` columns.
  - **Phase 2 — Migration `20260429100000_unify_portal_q_attribution.sql`.** Added `self_reported_source TEXT` column to lead_attribution. Backfilled 46 attribution rows by COALESCE-update from portal_questionnaires UTM/how_found. INSERTed 59 new attribution rows for portal_questionnaires that had no row yet. Idempotent. Applied via SQL Editor (not CLI) — file in repo for reference. Also created `20260429110000_admin_get_all_attributions.sql` RPC (admin-only SECURITY DEFINER returning all lead_attribution rows).
  - **Phase 3 — UI.** New shared helpers in `js/admin/admin-utils.js`: `formatLeadSource`, `renderSourceChip`, `buildSourceFilterOptions`, `leadMatchesSourceFilter`, `refreshSourceFilterDropdown`, `setSourceFilter` + `SRC_HE` / `SRC_COLORS` dictionaries. Helper falls back COALESCE: `last_utm → first_utm → row.utm → how_found → referrer → "ישיר / לא ידוע"`. `js/admin/admin-state.js`: new `attributionMap` (Map keyed `${linked_table}:${id}`), `currentSourceFilter` (per-table filter state), `loadAttributionMap()` runs sequentially before parallel loads. Updated all 5 lead renderers (admin-patients/therapists/leads/leads renderContactLeads/pipeline/portal-questionnaires) to render the chip + apply source filter. Added column header "מקור" + `<select class="source-filter">` to admin.html for each table. CSS: `.source-chip` and `.source-filter` styles in admin-styles.css. Two commits: 7e03d07 (helpers + patients), 7a87f69 (4 remaining tables).
  - **Bonus from earlier in same session — Discrete admin-dot footer link** (commit 5b11c00): added barely-visible "·" character at end of copyright line on 8 public footer pages, linking to `pages/admin.html`. Styled at 0.12 opacity, reveals gold on hover. Auth guard at `admin-auth.js` gates actual access.
  - **Open issue surfaced at end of session:** Hillel checked production and saw most chips showing "ישיר / לא ידוע" even for recent portal-q fillers (April 22-24). Explore agent identified two candidate root causes: (a) `admin_get_all_leads()` RPC may not include questionnaire's own utm_source columns in its SELECT (so `row.utm_source` fallback is undefined), or (b) new portal_questionnaire INSERTs don't auto-create lead_attribution rows so neither `last_utm_source` nor `row.utm_source` populates for fresh leads. Hillel decided to open a fresh Claude Code session for the investigation + Meta Ads ROI integration + unified decision dashboard. He was given a self-contained Hebrew prompt to paste.
- **Prior task — CRM lead coverage cleanup (2026-04-28, third commit):**
  - **Workflow fix (silent no-op):** `markHeat`/`logCall`/`changePortalQStatus` in `js/admin/admin-portal-questionnaires.js` now route to `contact_requests` when `lead_source='contact_form'`, else `portal_questionnaires`. Field-name mapping (`last_called_at` ↔ `last_contacted_at`). Status mapping admin→bot semantics (`new/potential/client` ↔ `new/contacted/converted`). Stamps `last_contacted_at` on non-new statuses.
  - **Migration `20260428200000_contact_requests_caller_columns.sql`:** adds `heat_level` + `call_count` + `caller_notes` to `contact_requests`. Reuses existing `last_contacted_at` / `contacted_by` (already written by crm-bot).
  - **`whatsapp_manual` filter:** added to `pq-filter-request-type` dropdown + `requestTypeLabel()` map. Bot-entered leads now searchable.
  - **Silent leads:** new `🔇 שקטים` option in `pq-filter-lessons`; condition: `lead_source='profile' && !has_questionnaire && completed_count===0`. Overview "ניהול לידים" card gets a 4th red stat `ov-leads-silent`.
  - **Email TLD validation:** `pages/course-library.html` in-portal training form now whitelists common TLDs (.com/.co.il/.org/etc.) to catch typos like `gmaiil.con`. `training.html` has no email field, no validation needed.
  - **Bot alignment (`crm-bot/`):** `src/templates/leads.js` `TYPE_LABELS` extended with 5 entries (training/whatsapp_manual/manual/portal_questionnaire/course-feedback). `src/services/lead-service.js` fallback `request_type` from `'manual'` → `'general'`. `src/ai/query-engine.js` schema extended with caller columns.
  - cache-buster bumped to `?v=7`.
- **Prior task — Training-leads UX organization (2026-04-28):**
  - **New sidebar nav-item "🎓 לידי הכשרה"** (`pages/admin.html`): routes to portal-q view with `request_type='training'` filter pre-applied + sort by date desc. Badge turns red with weekly-count when fresh leads waiting (gold + total otherwise).
  - **Inline Pipeline button per row** (`js/admin/admin-portal-questionnaires.js` renderPortalQuestionnaires): added 10th column "פעולות" with WhatsApp link + gold `fa-filter-circle-dollar` button → `movePortalQToPipeline()`. Hidden when status='client'. Colspans corrected from 11 → 10.
  - **Overview card on home** (`pages/admin.html`): new prominent gold-bordered "לידי הכשרה" card showing total/this-week/uncalled with "חדש" red badge when this-week > 0. Sits before "ניהול לידים" card.
  - **`enterTrainingLeadsView()` / `enterAllLeadsView()`** globals (admin-portal-questionnaires.js): orchestrate switchView('learning') + filter set + page-title swap + table-title swap. Wait-loop for lazy-load on first visit.
  - **`updatePqStats()`** extended: computes training totals/week/uncalled and writes badges (sidebar `#training-leads-badge`, overview `#ov-training-*`, `#ov-training-new-badge`).
  - **course-library.html in-portal training form** (line 8350): added mirror_table to sales_leads + UTM/landing_url capture (matches training.html pattern).
  - cache-buster bumped to `?v=6`.
- **Prior task — CRM training program lead visibility fix (2026-04-28):**
  - **Bug 1 — RPC `admin_get_all_leads()` v3** (`supabase/migrations/20260428100000_admin_get_all_leads_v3.sql`): contact_request data (`request_type`, `message`) now LEFT JOIN LATERAL'd onto profile rows, so registered users who later filled the training form get the "טופס הכשרה" badge. Source 2 dedup also matches by email (not just phone). Before: training leads from registered users were invisible — phone-dedup excluded their contact_request and source 1 hardcoded `request_type=NULL`.
  - **Bug 2 — `pages/training.html` mirror to sales_leads** (line 684): the training landing form now dual-writes via `submit-lead` Edge Function's `mirror_table` mechanism (contact_requests + sales_leads). Before: only contact_requests, so 0 of the 8 training leads in the backup reached Pipeline. Also captures UTM + landing_url.
  - **Bug 3 — request_type filter** (`pages/admin.html` + `js/admin/admin-portal-questionnaires.js`): new dropdown "סוג ליד" in ניהול לידים tab (training/patient/general/course-feedback/portal_questionnaire). cache-buster bumped to `?v=5`.
  - **Deploy:** code via `git push origin master` (Vercel auto-deploy), migration via `npx supabase db push --include-all`.
- **Prior task — Course-library proposed UX redesign (2026-04-26, single big PR):**
  - **Header:** New primary CTA "המשך בשיעור הבא" (deep-petrol gradient, gold play icon, shows next-uncompleted lesson title). Old gold "תוכנית הכשרה" CTA demoted to outline secondary. "חזרה לאתר" → icon-only with title tooltip.
  - **Sidebar progressive disclosure:** `renderSyllabus()` now opens only the module containing `currentModuleIndex` (or first module with unfinished lessons for new users). `selectLesson()` collapses all other modules. New "↓ קפיצה לשיעור הנוכחי" teal pill appears once a lesson is active; scrolls active lesson into view + 1s gold pulse animation.
  - **Continue Learning hero:** Replaces old `.wd-banner`. Deep-petrol gradient with subtle SVG grid pattern. 220px thumbnail (mqdefault.jpg from YouTube CDN) + content (label/title/meta/CTA/progress bar). Three states: first-start ("התחילו את המסע"), in-progress ("השיעור הבא שלך"), all-done ("כל הכבוד! סיימת את הקורס").
  - **Quick stats row:** 3 cards — 🔥 ימי רצף (`localStorage('study_streak')` with consecutive-day logic), ⏱️ זמן שנותר (sums `parseDuration(lesson.duration)` for uncompleted), 📚 מודול נוכחי (e.g. `3/8`).
  - **Compact dashboard cards:** Bulletin collapsed-by-default (60px tall, "ראה הכל" toggle expands). Intro video has "צפיתי ✓" button → `localStorage('intro_video_watched')` flips it to mini banner with "פתח שוב". New compact `wd-affiliate-compact` card linking to full ambassador dashboard. Leaderboard rebuilt as 3-column horizontal podium (medal/name/count) with "תודה על ההפצה" footer.
  - **Community empty state:** Replaced bare "אין פוסטים" with chips ("💡 שתפו תובנה" / "❓ שאלה" / "🎯 חוויה") + post button that focuses the composer.
  - **Notes split view:** New floating "📝 הצג הערות" toggle on lesson view (top-left of `.lesson-split-main`). Side panel `<aside id="lessonSplitNotes">` with second textarea `#notesSideTextarea` synced bidirectionally with the original `#notesTextarea`. State persists in `localStorage('notes_panel_open')`. CSS Grid: `.video-section.notes-open { grid-template-columns: 2fr minmax(320px,1fr) }`. Notes tab hidden on desktop ≥1281px (still visible on mobile).
  - **Lesson end panel:** "סמן כהושלם" toggle button below the nav bar — `toggleLessonComplete()` adds/removes from `completedLessons` array. Tip shows auto-mark behavior (45s).
  - **Focus mode:** New top-right toggle on `.lesson-split-main`. `body.focus-mode` hides header + sidebar + share-banner + feedback FAB; video fills the viewport. Esc exits.
  - **Keyboard shortcuts:** `setupKeyboardShortcuts()` wired on DOMContentLoaded. `←` next, `→` prev (RTL), `Space` play/pause via YT IFrame API, `F` focus mode, `N` notes panel, `Esc` exit focus/close notes, `Ctrl+S` insert `[mm:ss]` timestamp into notes from current video time.
  - **YouTube IFrame API:** Async-loaded in `<head>`. `playVideo()` adds `enablejsapi=1&origin=...` to embed URL. `tryAttachYtPlayer()` creates `window._ytPlayer = new YT.Player('videoPlayer', ...)` after iframe loads. Works for keyboard space + Ctrl+S timestamps.
  - **Floating buttons cleanup:** Feedback FAB compacted to 44px icon-only circle. Mobile sidebar toggle untouched (already desktop-hidden).
  - **No backend changes** — all UI. No migrations, no Edge Functions touched.
  - **Validation:** Tag balance OK (354 div, 75 button, 14 script, etc.). Inline JS parses cleanly via `new Function`. File grew 7,829 → 9,135 lines.
- **Multi-day overhaul (2026-04-16 → 2026-04-20, 30+ commits) — prior:**
  - **Dashboard consolidation:** 16 tabs → 9. Sidebar: ניהול לידים → Pipeline → לקוחות משלמים → מיזם → מקורות תנועה → כלים. Merged traffic sub-tabs (Attribution/GA4/IG/Campaigns).
  - **Unified leads:** New RPC `admin_get_all_leads()` shows ALL profiles + contact_requests in one table. Source column, inline status dropdown, advanced filters (gender/age/city/date/occupation).
  - **Data accuracy:** Replaced hardcoded hero stats with real Supabase RPC. Fixed Google leads logic. Badge defaults "…" instead of fake "0". Promise.allSettled for error resilience. GA4 cache invalidation.
  - **Social cause integration:** 3 legal docs (patient/therapist agreements + privacy policy) uploaded to Supabase Storage. PDFs embedded in patient-step4 + therapist-step4. `agreement_signed_at` tracking. registration.html redirects to patient-step1. Fixed missing patient columns (military_role, referral_source, session_style, photo_url, commitment_confirmed, truth_confirmed). WhatsApp number updated to 972512940781.
  - **Popup system fixes:** Cooldown minutes→ms conversion. Anon RLS for popup_configs. Scheduled status fix. auth_wall DB row added. Training CTA popup after 5 lessons.
  - **Performance:** Deferred 6 scripts. Lazy images. Community comments limit 500→100.
  - **Design system:** Glass button components (btn--gold/teal/clear/purple/dark/icon/pill) in components.css. LMS portal demo at /demo-portal/ with 3 variations.
  - **CRM bot:** Updated report templates (ניהול לידים/Pipeline/משלמים structure). Added diagnostic button (stethoscope) for automations.
  - **Admin fixes:** Modal overflow fix. Portal-q title→"ניהול לידים". Pipeline points to sales_leads (not funnel). Popups tab restored. Training CTA button in course header.
- **Next planned task:** (a) **Phase 3 — unified ROI dashboard** in admin: new sidebar tab joining lead_attribution × meta_campaign_spend_daily × subscriptions × signed_contracts; one row per campaign/source with spend/leads/questionnaires/answered/contracts/revenue/ROI%, color-coded for readability. (b) Fix `enqueue_qualified_lead()` v_profile RECORD bug (guard field access with `IF NEW.user_id IS NOT NULL`). (c) Implement LMS portal glass design. (d) Dark mode fixes for admin modals. (e) Deploy CRM bot updates to Fly.io.
- **Blocking issues:** Fly.io auth expired (need `fly auth login`). Hillel needs to add UTM to Meta ad URLs (or update `meta_campaign_to_utm` row) before Phase 3 ROI dashboard can show valid join.

## Recent Changes
| Date | What Changed | Files Affected |
|------|-------------|----------------|
| 2026-05-09 | v2 portal consolidation per Hillel: in-page ambassador (link/copy/WA share/leaderboard) + basic master (paid-customer-gated, 6 workbooks via signed URLs + 8 chapters of מפגש 1 in fullscreen viewer) + community dropped + free-portal/questionnaire redirect flipped to v2. Old course-library.html intentionally retained as fallback. Deploy `c435f9b`. | pages/course-library-v2.html, pages/free-portal.html, pages/portal-questionnaire.html |
| 2026-05-09 | Early Meta Pixel inline in `<head>` of free-portal.html + implicit-consent model in marketing-tools.js (tracking ON unless user explicitly opts out via "ביטול מעקב"). Fires `init`+`PageView` before fonts/SDK/auth, bails out early on `cookie_consent_v2='essential'`. Banner copy rewritten — informational, not a gate. Opt-out additionally fires `fbq('consent', 'revoke')`. Fixes the ~78% LPV gap that made the Meta=X/CRM=Y daily report unreliable. Deploy `8aab76f`. | pages/free-portal.html, js/marketing-tools.js |
| 2026-05-04 | NLP Retention Phase 2 + Phase 3 + 2 iterations live: admin approval tab with vision-quote modal + edit textarea + 7 status filters; trigger button calls Edge Function `retention-run` (full Python→Deno/TS port: lessons_map embedded, deterministic classify, parallel batches of 3, dedup race-safe, prompt caching) — hard cap 30/click; standalone save-edits button (Save ≠ Approve); em-dash banned in generation (rule + body cleanup). Hash deeplink router for all admin tabs. New migration adds admin RLS on retention_messages (SELECT + UPDATE). 3 commits, all deployed. | js/admin/admin-retention.js (new), js/admin/admin-utils.js, js/admin/admin-state.js, pages/admin.html, supabase/migrations/20260503130000_retention_admin_rls.sql (new), supabase/functions/retention-run/index.ts (new) |
| 2026-05-03 | NLP game deep-fix: cross-device progress reset (P0 — `loadPlayerData` was omitting `migrationVersion` so `migrateProgress` wiped `completedLessons` on every fresh-Supabase load), end-of-module dead-end CTA, 4 fields silently lost on cross-device sync (perfectLessonsList/weeklyActivity/moduleAccuracy/longestStreak), unified module-lock criterion to "previous=100% OR own progress" (was 50% on home / 100% in module — inconsistent), new `showError`+`isModuleLocked`+`initBeforeUnload`+`findNextLesson/Module` helpers, defensive null-checks on startLesson + renderExercise, beforeunload Supabase save flush. **NOT YET DEPLOYED** — awaiting Hebrew brief + Hillel OK. | js/nlp-game.js, supabase/migrations/20260503140000_nlp_game_player_full_state.sql (new) |
| 2026-04-30 | Phase 3 ROI initiative: unified "מקור הלקוחות" dashboard replaces split GA4 + lead_attribution + Instagram views. New SECURITY DEFINER RPC `admin_unified_source_funnel` returns funnel JSONB (visitors/leads/registrations/paid per source) using DISTINCT-person logic (hindsight 2026-04-19). how_found free-text bucketed via ILIKE Hebrew+English patterns; mismatch detection (UTM vs how_found). 5 transparency banners + per-column tooltips + healthy-range color coding directly in UI per Hillel's "כל פער ייכתב בתוך הדשבורד עצמו" requirement. Old tabs preserved as `(ישן)` sub-tabs for 2-week sunset. Cache-buster bumped to `?v=8`. | migration 20260430120000 (new), js/admin/admin-sources.{js,css} (new), js/admin/admin-utils.js, pages/admin.html |
| 2026-04-30 | Phase 2 ROI initiative: fix "ישיר / לא ידוע" attribution bug. Added AFTER INSERT triggers on portal_questionnaires + contact_requests to auto-create lead_attribution rows for new leads (root cause of bug — 29.4 backfill was one-shot, no trigger). RPC `admin_get_all_leads()` v4 adds utm_medium + utm_campaign columns (v3 dropped them; chips lost the medium suffix in fallback path). Idempotent gap-window backfill caught 61 portal_q rows from 29.4 → 30.4. No frontend change — `formatLeadSource()` already handles fallback chain correctly. Verified via `scripts/verify_phase2_attribution.py`. | migrations 20260430090000 + 20260430090100 (new), scripts/verify_phase2_attribution.py (new) |
| 2026-04-29 | Phase 1 ROI initiative: Meta Ads daily spend integration. New tables meta_campaign_spend_daily + meta_campaign_to_utm with admin RPCs. Edge Function fetch-meta-ads-spend deployed (calls Meta Marketing API v25 /insights per-day, counts only `lead` action_type to dedupe Meta's 6-way conversion duplication). Daily cron at 03:00 UTC. 14-day backfill executed: 7 active days, ₪413.88 total spend, 21 leads, numbers verified vs Python skill. Token regenerated 2026-04-29 (old one Meta-blocked code=200). | migrations 20260429120000 + 20260429120100 + 20260429120200 (new), supabase/functions/fetch-meta-ads-spend/index.ts (new), Supabase secrets (META_*) |
| 2026-04-29 | Traffic source unification + per-lead chip + filter in 5 admin tables; backfilled 46 + 59 portal_questionnaires attribution rows; added self_reported_source column; new admin_get_all_attributions RPC | migrations 20260429100000 + 20260429110000 (new), js/admin/admin-utils.js, js/admin/admin-state.js, js/admin/admin-patients.js, js/admin/admin-therapists.js, js/admin/admin-leads.js, js/admin/admin-pipeline.js, js/admin/admin-portal-questionnaires.js, pages/admin.html, css/admin-styles.css |
| 2026-04-29 | Discrete admin-dot footer link to CRM (admin entry without typing URL) | css/components.css, index.html, legal-gate.html, pages/about.html, pages/courses.html, pages/free-portal.html, pages/project-lobby.html, pages/questionnaire.html, pages/training.html |
| 2026-04-28 | CRM lead coverage cleanup: workflow updates routed by lead_source (silent-no-op fix), migration adds caller columns to contact_requests, silent-leads filter + overview stat, whatsapp_manual filter, email TLD validation, bot type labels + fallback aligned | migration 20260428200000 (new), js/admin/admin-portal-questionnaires.js, js/admin/admin-utils.js, pages/admin.html, pages/course-library.html, crm-bot/src/templates/leads.js, crm-bot/src/services/lead-service.js, crm-bot/src/ai/query-engine.js |
| 2026-04-28 | Training-leads UX overhaul: dedicated sidebar tab + overview card + inline pipeline button per row + fresh-week badge counter; course-library.html in-portal form also mirrors to sales_leads | pages/admin.html, pages/course-library.html, js/admin/admin-portal-questionnaires.js |
| 2026-04-28 | Training program lead visibility fix: RPC v3 surfaces contact_request data on profile rows, training.html mirrors to sales_leads (Pipeline), new request_type filter in admin | migration 20260428100000 (new), pages/training.html, pages/admin.html, js/admin/admin-portal-questionnaires.js |
| 2026-04-20 | Patient form fix: added missing columns (military_role, referral_source, session_style, photo_url, commitment_confirmed, truth_confirmed). Updated WhatsApp to 972512940781. Better submit-lead error messages. Modal overflow fix. LMS design demo deployed. | patient-step4.html, admin-styles.css, demo-portal/*, 2 migrations |
| 2026-04-19 | Dashboard consolidation (16→9 tabs), unified leads RPC, social cause legal docs, popup fixes (cooldown/RLS/scheduled/auth_wall), training CTA popup, performance (defer scripts), community audit, automation diagnostic button, CRM bot report templates updated | admin.html, admin-utils.js, admin-portal-questionnaires.js, admin-popups.js, popup-manager.js, course-library.html, components.css, crm-bot/analytics+reports, 10+ migrations |
| 2026-04-16 | Analytics accuracy overhaul: real hero stats, traffic tab wired, demographics, error handling, Google leads fix, badge defaults | index.html, admin.html, admin-segments.js, admin-analytics.js, admin-state.js, migrations |
| 2026-04-14 | Segments: abandonment KPI + re-engage shortcut. Audit verdict = abandonment, not leak. Commit fef72b9. | migration 063 (new), js/admin/admin-segments.js, js/admin/admin-automations.css |
| 2026-04-14 | Segments: count from all 5 registration tables (fix "אתמול 0" bug — profiles-only query was missing anonymous form tables). Commit 3ddb4db. | migration 062 (new), js/admin/admin-segments.js |
| 2026-04-14 | Segments: flat theme-aware redesign for accessibility (var(--card)/var(--text) instead of hardcoded aqua tints). Commit a1254f8. | js/admin/admin-automations.css, js/admin/admin-segments.js |
| 2026-04-14 | Segments & Filters tab: KPI strip + 4 breakdown panels + custom filter builder backed by `admin_segments_overview()` RPC. Commit f958fea. | migration 061 (new), js/admin/admin-segments.js (new), js/admin/admin-automations.css, js/admin/admin-utils.js, pages/admin.html |
| 2026-04-14 | Smart Automations UX polish: live match count + card audience badges + brand palette sweep + clone/cron-validation/race-guard/dedup. Commit ba5e255. | js/admin/admin-automations.js, js/admin/admin-automations.css |
| 2026-04-13 | Smart Automations Engine: rule builder admin tab + crm-bot engine + 2 migrations. Commit a98e560. | migrations 058 + 059 (new), js/admin/admin-automations.{js,css} (new), pages/admin.html, js/admin/admin-utils.js, crm-bot/src/services/automation-engine.js (new, deployed to Fly), crm-bot/src/scheduler/cron.js, crm-bot/server.js |
| 2026-04-13 | Backup hardening: 35 tables, try/except, WhatsApp alerts, run log, watchdog, fixed Task Scheduler battery + Hebrew path issues | scripts/backup-supabase.py, scripts/check_backup_health.py (new), CLAUDE.md, Windows Task Scheduler (BeitVmetaplim-DailyBackup + BeitVmetaplim-BackupHealthCheck) |
| 2026-04-12 | Popup system v2: anonymous session tracking, A/B variants, cross-device dismissals, status lifecycle, insights log, Claude Code export, funnel metrics, 7-day sparklines, bulk ops, CSV export, iframe preview | migrations 054-057, popup-manager.js, admin-popups.js, admin.html, course-library.html, pages/popup-preview.html (new), docs/popup-insights.md (new), .vercelignore |
| 2026-04-09 | Security phase 2: auth/CORS/XSS/privilege escalation/Turnstile/Sheets token/404.html/vercelignore/auth race | migrations 052-053, 5 Edge Functions, admin-paid.js, admin-auth.js, admin-state.js, supabase-config.js, .vercelignore, 404.html |
| 2026-04-07 | Security phase 1: RLS policies, column REVOKE, admin RPC functions | migrations 049-051, 5 admin JS files (use db.rpc instead of select('*') for sensitive tables) |

## Popup System v2 — Quick Reference
- **Anonymous event logging:** PopupManager generates session_id in sessionStorage (`popup_session_id`) and inserts popup_events with user_id=NULL. RLS policy in migration 054 allows this only when session_id is present.
- **Triggers:** `PopupManager.notifyLessonComplete()` / `notifySignup()` / auto-fired `page_load` / `login` (on auth transition). Admin creates popups with `trigger_event` column, no code changes needed.
- **A/B tests:** Set `variant_group` + `variant_label` on multiple popups. PopupManager picks sticky per session (sessionStorage `popup_variant_choice`).
- **Cross-device dismiss:** `popup_dismissals` table synced to localStorage `popup_history` on login.
- **Claude Code analysis:** Admin clicks "ייצא לקלוד" → downloads JSON → gives to Claude Code with `docs/popup-insights.md` playbook → Claude inserts findings into `popup_insights_log` table → they appear in admin dashboard timeline.
- **Admin notes field:** Every popup has `admin_notes` — hypothesis/goals for Claude to read first.

## Security Architecture (post-audit)
- **Sensitive columns REVOKEd** from `authenticated` role: `therapists.questionnaire`, `therapists.signature_data`, `questionnaire_submissions.{weakness,challenge,what_is_therapist,what_touched_you}`, `portal_questionnaires.{motivation_tip,main_challenge,vision_one_year}`, `signed_contracts.{signer_id_number,signature_data}`
- **Admin access pattern:** JS uses `db.rpc('admin_get_*_full')` SECURITY DEFINER functions (verify `profiles.role='admin'`) to read sensitive data
- **Edge Functions:** All use explicit ALLOWED_ORIGINS whitelist (no wildcard CORS). gemini-mentor requires auth + 100/day rate limit via `ai_chat_usage` with `source='mentor'`
- **Turnstile:** submit-lead + submit-contract log CRITICAL error if `TURNSTILE_SECRET_KEY` missing (instead of silently disabling)
- **Privilege escalation blocked:** Migration 052 `WITH CHECK` on profiles UPDATE policy prevents self-role-change
- **Sheets API token:** Moved from `supabase-config.js` to Supabase Secrets, accessed via `sheets-sync` Edge Function (admin-only)

## Active Branches
- `master` — production, auto-deploys to Vercel on push

## Environment Notes
- **Deploy JS changes:** `git push origin master` → Vercel auto-deploy
- **Deploy Edge Functions:** `npx supabase functions deploy <name> --no-verify-jwt`
- **Apply migrations:** `npx supabase db push --include-all`
- **Set secrets:** `npx supabase secrets set KEY=value` (use `--env-file` for Windows-safe JSON)
- **Docker:** NOT required for deploys (warning but succeeds). Required only for local `db dump` / `db reset`

## Open Questions
- Admin auth via `tempClient` (admin-auth.js:10) creates a second Supabase client — could be consolidated with `db` from admin-state.js
- `therapists.user_id` column does not exist in live DB — matchmaking platform only, no therapist login. Migration 050 dropped the dead policies.
