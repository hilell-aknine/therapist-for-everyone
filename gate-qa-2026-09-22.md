# gate-qa-2026-09-22 · בית המטפלים

> ## 🔁 הרצה שנייה (אחרי תיקון) — 22.09.2026
> **חוסם 1 תוקן ואומת מקצה לקצה מקומית. `origin/master` עדיין נושא את הפגם עד שהלל יפרוס.**
>
> **התיקון:** שער חדש ב-`js/install-prompt.js` — ההזמנה להתקנה לא נפתחת כשיש שדה קלט פעיל ברבע התחתון של המסך, ואם היא כבר פתוחה והמבקר גולל לתוך טופס היא **מפנה מקום בלי לרשום סירוב** וחוזרת כשהוא מתרחק.
>
> | מסלול | תוצאה | ראיה |
> |-------|-------|------|
> | CTA ⇐ `#register`, המתנה מעבר לכל הטריגרים | PASS | הבאנר לא נפתח כלל. שלושת השדות מחזירים `INPUT` ב-`elementFromPoint`. תנאי ההפעלה אומתו כמתקיימים (גלילה 83%, 51 שניות בדף, `hasEvent: true`) — כלומר זו שלילה אמיתית ולא היעדר טריגר. |
> | מתרחקים מהטופס | PASS | הבאנר נפתח. השער מפנה מקום, לא משתיק. |
> | באנר פתוח ⇐ גלילה לתוך הטופס | PASS | תוך פחות מ-700ms: `data-ip-yield="1"`, `visibility: hidden`, **וכל ארבעת השדות (שם, מייל, טלפון, אישור תנאים) מחזירים `INPUT`**. |
> | חזרה למעלה | PASS | הפינוי מתבטל, הבאנר גלוי ופתוח. ההזמנה לא אבדה. |
>
> **צילום:** `qa-screenshots\mobile-390-register-FIXED.png` — הטופס פנוי, מול `mobile-390-register-form.png` שבו הבאנר יושב עליו.
>
> **🔴 שתי רגרסיות בתיקון שלי עצמו, שנתפסו במדידה ולא בהנחה:**
> 1. הגרסה הראשונה תלתה את הפינוי ב**אירוע גלילה**. נמדד שבדף הזה נורה **אירוע אחד בלבד על פני שתי גלילות מלאות** (קפיצת עוגן וגלילה תכנותית לא יורות `scroll` אמין), ולכן הפינוי כמעט אף פעם לא רץ. הוחלף בשעון 300ms שרץ **רק כל עוד הבאנר פתוח** ומכבה את עצמו.
> 2. הגרסה השנייה הדליקה את השעון רק במסלול `deferred`. טעינה שבה `beforeinstallprompt` לא נורה הציגה את אותו באנר דרך `showFallback()` **בלי שומר**, והתקלה חזרה במלואה. השומר הועבר לאחרי שלושת המסלולים.
>
> **נוסף לצורך אימות:** `InstallPrompt.zone()` ו-`InstallPrompt.yieldNow()` נחשפו ב-API הבדיקות שכבר קיים בקובץ, כדי שאפשר יהיה לאמת את השער בדפדפן במקום להסיק אותו.
>
> **⚠️ ממצא חדש שלא תוקן (חומרה נמוכה, אינו חוסם):** בגלילה קיצונית שבה שדה נדחק לתחתית המסך המוחלטת, **סרגל הסכמת העוגיות** (z-index 9999, גובה 32px) יושב עליו. זה רכיב אחר, ואסור להסתיר ממשק הסכמה. התיקון הנכון הוא ריפוד תחתון לדף כל עוד הסרגל מוצג, ולא הסתרה. **הועבר להכרעתך.**
>
> **מה עדיין לא הורץ:** מטריצת QA המלאה (תפקיד × שפה × מסלול-כתיבה). ההרצה הזו אימתה את חוסם ה-`reality-check` בלבד.
> **verdict אחרי התיקון: PASS על החוסם · השער כולו נשאר פתוח עד שתרוץ המטריצה.**

---

## ההרצה הראשונה (לפני התיקון)

verdict: **FAIL**
reality-check: **FAIL** — ❌ באנר ההתקנה (PWA) מכסה פיזית את שדה הטלפון ואת תיבת אישור התנאים בטופס ההרשמה ברוחב טלפון, כלומר הפעולה המרכזית של מבקר חדש חסומה עד שהוא מזהה את הבאנר ומסלק אותו.

**מה נבדק:** הלינק הציבורי שהמשתמש באמת מקבל — `https://www.therapist-home.com/` ו-`/pages/course-library-v2.html` — בדפדפן אמיתי, פרופיל קר, 390×844, אנונימי.
**מה לא נבדק ולמה:** לפי חוזה השער, `reality-check: FAIL` עוצר מיד ואין להמשיך למטריצת תפקיד×שפה×מסלול-כתיבה. **המטריצה לא הורצה.** בנוסף לא בוצעה הרשמה אמיתית ולא נכתבה אף שורה למסד — זו מערכת חיה עם לומדים אמיתיים, ובדיקת כתיבה הייתה מייצרת רשומת זבל בייצור.

| בדיקה | תוצאה | ראיה |
|-------|-------|------|
| 1. הלינק האמיתי, קר | PASS | ניווט אנונימי ל-`/pages/course-library-v2.html` הופנה ל-`/pages/free-portal.html`. שער ההרשאות עובד, אין דליפת תוכן ללא התחברות. |
| 2. רוחב טלפון 390px | PASS | שלושה צילומים ב-`qa-screenshots\`: `mobile-390-home.png`, `mobile-390-portal-anon-gate.png`, `mobile-390-register-form.png`. `document.scrollWidth = 375 ≤ 390` — אפס גלישה אופקית. אין דף לבן. |
| 3. RTL במבחן ספרה | PASS | עוגן: "קבלו גישה לפורטל לימודים בשווי **7,200 ש״ח**" — הספרה יושבת נכון בתוך המשפט העברי. |
| 4. הדאטה שעל המסך | **WARN** | ✅ "+51 סרטונים ערוכים" תואם את 51 שיעורי הפרקטישנר במסד. ⚠️ **סתירה בין שני דפים באותו אתר:** דף הבית מציג "+30,000 צפיות בקורסים שלנו" ודף הפורטל מציג "+25,000 צפיות בקורס". ⚠️ "+350 מטפלים שהכשרנו" אינו ניתן לאימות מהמסד (11 לומדים סיימו 40+ שיעורים) — ייתכן שהוא מתייחס להכשרות פרונטליות של רם, ולכן **לא נקבע שהוא שגוי**, אבל הוא דורש אישור רם. |
| 5. קונסול נקי | PASS | אפס שגיאות מקוד שלנו. שתי השגיאות היחידות מגיעות מסקריפט ה-Turnstile של Cloudflare עצמו (`challenges.cloudflare.com`), צד שלישי. |
| 6. ניגודיות וקריאות | PASS | נבדק על צילומי המסך בפועל: לבן וזהב על פטרול כהה, קריא. הערה לא-חוסמת: תווית ה-kicker "הכל כלול בפנים" בטורקיז עמום היא הניגודיות הנמוכה בעמוד, והיא גם סוג התווית שהלל ביקש להימנע ממנו. |
| 7. הפעולה המרכזית עד הסוף | **FAIL** | לחיצה על "אני רוצה גישה לפורטל" קופצת נכון ל-`#register` והטופס נטען. **אבל `document.elementFromPoint` על מרכז שדה הטלפון מחזיר `DIV.ip-banner is-open` ולא את ה-`INPUT`**, ועל תיבת אישור התנאים מחזיר `BUTTON.ip-btn`. הבאנר תופס `top:635 → bottom:844`, גובה 209px, z-index 8500 — כלומר את הרבע התחתון של מסך הטלפון, בדיוק היכן שיושבים שני השדות האחרונים. |

## המנגנון (למה זה קורה דווקא שם)
`js/install-prompt.js` מפעיל את הבאנר בסף `scrollPercent: 45`. לחיצה על ה-CTA הראשי מדלגת ל-`#register`, שחוצה את הסף — ולכן **תנאי ההפעלה של באנר ההתקנה מתלכד בדיוק עם הגעת המבקר לטופס ההרשמה**. זו לא תקרית עיצוב אקראית אלא צירוף מובנה שיחזור אצל כל מבקר חדש בטלפון.

נוסף על כך יושב סרגל הסכמת העוגיות ב-z-index 9999 מעל הכל, כך שבמסך ההמרה הראשון יש **שתי שכבות חופפות** מעל הטופס.

## blockers
1. 🔴 **חוסם:** באנר ההתקנה חוסם את שדה הטלפון ואת אישור התנאים בטופס ההרשמה בטלפון. שניהם שדות חובה להרשמה.
   **התיקון המוצע:** להשתיק את הבאנר כל עוד סקשן ההרשמה נמצא בתצוגה (IntersectionObserver על `#register`), במקום לשחק ב-z-index. שינוי z-index רק יחליף מי מכסה את מי.
   **לא לתקן בלי אישור:** זו מערכת חיה, והפריסה נשארת אצל הלל.
2. 🟡 **דורש הכרעת תוכן:** לאחד את מספר הצפיות בין דף הבית לדף הפורטל (30,000 מול 25,000), ולאשר מול רם את "+350 מטפלים שהכשרנו".

## מה נדרש כדי לפתוח את השער
לתקן את חוסם 1, להריץ מחדש `reality-check` על אותו לינק בפרופיל קר, ורק אם הוא מחזיר PASS — להמשיך למטריצת QA המלאה (תפקיד × שפה × מסלול-כתיבה) שטרם הורצה כאן מעולם.

signed: Claude · 2026-09-22 · אישור הלל: ____
